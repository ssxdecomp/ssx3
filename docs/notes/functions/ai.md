<!-- Generated from the SSX 3 port notes; see docs/notes/README.md. -->

# Function notes: src/ai/

## `src/ai/computer.cpp`

Segment `0x0010C4F8`-`0x00113198`.

- **`0x0010CAD8`** - Computer-rider (NPC) grab chooser: picks among normal/tweak/uber grab mappings using usage counters, capability, boost tier, stat timing and the shared RNG. *[static]*
- **`0x0010D1A0`** - Computer-rider ground path/recovery state update; the first call made by `0x0010A960`. *[static]*
- **`0x0010D410`** - Computer-rider route scoring (vtable slot +0xA4), used by the computer branch `0x00112A50` of route progress. Scores candidates by affinity, speed thresholds, proximity, an occupied-route penalty, followed-target priority and a random branch; context fields owner+0xE00 role, +0xE04 allow flag, +0xE08 randomize. At zero speed it relies on EE float saturation (x/0 gives +/-FLT_MAX, 0*MAX = 0). *[PS2]*
- **`0x0010DA10`** - Computer-rider interaction decision routine in the ground producer. *[static]*
- **`0x0010DBF0`** - Computer-rider interaction decision (attack/defense) in the ground producer; attacks are enabled toward riders with relationship level >= 3. *[static]*
- **`0x0010DEF0`** - Computer-rider pacing (rubber band), working with `0x00120090`. Uses the actor+0x4D0 remaining distance for itself and for the first enabled human pair record; a signed event variant is selected from `0x00535C11`. *[PS2]*
- **`0x0010E028`** - Requests a rider upper-body reaction/notification by kind: sets rider+0x358 = kind and +0x354 = due logic tick; the reaction is played later by `0x00115B58`. Kinds: 1 great trick, 2 Uber landing, 3 combo payout, 4 finish/get-up (clip/reaction 314), 5 full meter, 6 timer end. Also used as the boost-expiry notification from `0x001200D0`. *[PS2]*
- **`0x0010E098`** - Boost meter award (rider, category mask, delta). Policy 3 or zero delta returns; a positive delta calls `0x00149690` (result unused) and must pass the category test against rider+0xB28 (negative deltas bypass it). An eligible change calls `0x0029AB08(old meter, delta)` (probably the fill-tick sound event) and clamps the meter to [0,1]. A full meter with +0xB2C enabled requests notification 5 when superTime is 0, a nonpositive tier increments once, and tiers 1..9 extend superTime (+0x2F0) to >= 20 s (Tricky); a negative delta clears superTime below tier 10. Requests reaction 5 via `0x0010E028`. Callers: landings, rail scoring/Uber completion, bail (-0.25), KO attack (1.0, category 2), reset penalty, multiplier pickup. *[PS2]*
- **`0x0010E228`** - Rider-pair soft bump event: adds 1 to the pair relationship via `0x00155BF0`, plays the bump sound `0x00298488` and hit speech. *[PS2]*
- **`0x0010E2E8`** - Rider-pair crash contact event: adds 2 to the pair relationship via `0x00155BF0` and plays the bump sound `0x00298488`. *[PS2]*
- **`0x0010E3A8`** - Rider-pair soft attack event: adds 4 to the pair relationship via `0x00155BF0` and plays the attack sound `0x00298138` (source of Hit/Aggression rider speech). *[PS2]*
- **`0x0010E468`** - Rider-pair crash attack (knockout) event: adds 6 to the pair relationship via `0x00155BF0`, plays the attack sound `0x00298138`, runs `0x00119400` on the attacker (records a KO in score object +0x128) and awards the attacker 1.0 meter (category 2) through `0x0010E098`. *[PS2]*
- **`0x0010E558`** - Rider-side checkpoint/race bonus hook, called between `0x00112FB0` and `0x001194C0`: copies the place (+0xE8 = +0xEC) and, in slope style (`0x00535C12` == 1) unless bit 9 of `0x005308D0` is set, calls `0x001194C0`. *[PS2]*
- **`0x0010E5D8`** - Course finish eligibility test: a finish counts only on course 1 (Metro-City) or courses 5..13, so there is no free-ride finish on Snow Jam or Happiness and Peak runs always end at Metro-City (race) or a freestyle course (Peak 2 Race cannot end at Intimidator). GMM+0x6C is not read. Also described as the course event callback that sends runtime event 11 (checkpoint) to `0x00270AB0`. *[static]*
- **`0x0010E770`** - Speed-boost pickup award (rider effect type 1, rider+0x6C0 slot +0x6C): adds f12 to rider+0x2E8, then sends feedback through `0x0028B180` to the pickup sound dispatcher `0x0029CED8` (kind 0, scalar 1) and to `0x002A3B18` (type 1). *[PS2]*
- **`0x0010E7D0`** - Trick-boost pickup award (rider effect type 2, rider+0x6C0 slot +0x74): adds f12 to rider+0x2EC, then feedback to the pickup sound dispatcher `0x0029CED8` (kind 1) and `0x002A3B18` (type 2). *[static]*
- **`0x0010E830`** - Multiplier icon pickup handler (stage effect type 3, x2/x3/x5/x10 icons; rider vtable +0x78): only in the air or on a rail (motion mode from `0x0011FE98` is 1 or 4) it calls `0x00119448`. *[PS2]*
- **`0x0010E8B8`** - Point-icon pickup handler (stage-script builtin27 effect type 6): calls the score pickup `0x00119608` and the pickup sound dispatcher `0x0029CED8`. *[PS2]*
- **`0x0010E910`** - Rider landing award: commits the trick score via `0x00119D40`, runs Uber tier progression, then passes the returned delta to the meter award `0x0010E098` (category 1) and clears rider+0x2EC when the award is positive. Reports the landing impact to the snow impact trigger `0x00111AA0` (strength uses the speed magnitude |v|, not the normal speed), plays the landing sound `0x002948D0`, sets the landing rumble (0.5 * speed) and requests reactions for human riders. Callers: landing `0x00139C88`, air rail attach `0x00106848` (motion 1, called as (rider, 0, style, +0x330, out, speed)), scenery land-on-top `0x001057B8`. *[PS2]*
  - `0x0010E9B4`: Uber tier progression block (to `0x0010E9F4`): a positive Uber-count change (+0x54) adds at most (10 - tier), so several Ubers in one air advance by the count (1 -> 3 -> 5); reaching tier 10 sets superTime to 60. *[PS2]*
  - `0x0010EA08`: Call to the snow impact trigger `0x00111AA0`: record+0 point, record+0x10 normal, f21 strength, record+0x4C surface. *[static]*
  - `0x0010EA28`: Landing reaction selection block (to `0x0010EAA4`), human riders only (+0x874): combo payout -> kind 3, Ubers -> 2, great trick (`0x00149778`) -> 1, via `0x0010E028`. *[PS2]*
  - `0x0010EAD8`: Landing rumble site: writes owner+0xDFC with 0.5 * the landing speed (covers board, rail and instance landings). *[PS2]*
- **`0x0010EB30`** - Hard crash entry, e.g. (rider, clip 358/359, 0, rider+0x438, block) from board press. Order: peak impact, crash count/boost penalty (`0x00119B08`), observers, transient control 13, optional prewind compensation (`0x0011DFE0`), presented-root bake (`0x0011FA10`), input resets, clip preview root (`0x0030ECD8`) and sequence-root offset (`0x00311BF0`), clip play, then control 8 and motion 2 (`0x0011FE78(2)` runs the old motion exit). Clears only the +0x1F0/+0x208/+0x250 triplets, +0x330 and failed trick points; rotates the crash root by rail style (+0x328); camera shake index 4 (`0x0012D160`); snow impact via `0x00111AA0` (strength = closing speed). Callers: `0x00105D98`, `0x00139C88`, `0x00130DD0`, `0x00107E70`. *[PS2]*
  - `0x0010EB78`: Crash rumble impact source. *[PS2]*
  - `0x0010EBA4`: Meter/boost scoring block inside the crash entry (probably the crash boost penalty). *[static]*
  - `0x0010EC04`: Call to `0x00111AA0` reporting the crash impact: event+0 point, event+0x20 normal, event+0x30 closing speed, surface in s2. *[static]*
- **`0x0010F1C0`** - Stage-script (builtin27) pickup / rider effect dispatcher for types 0..7 through jump table `0x00456AF0`: type 1 -> rider+0x6C0 slot +0x6C (`0x0010E770`, speed boost), type 2 -> slot +0x74 (`0x0010E7D0`, trick boost), type 3 multiplier icons -> rider vtable +0x78 (`0x0010E830`), type 6 point icons -> `0x0010E8B8` -> `0x00119608`. *[PS2]*
- **`0x0010F280`** - Quick-recovery path that calls the score recovery `0x00119BB0`. *[PS2]*
- **`0x0010F2D8`** - Shows a popup message (e.g. 'MISSION SUCCESS'). *[static]*
- **`0x0010F3B8`** - Rider pair record table initializer: enables an owner's records only when owner+0x880 == 7 and the other slot exists and differs; record +0x08 starts at 1e10, and other actors still get reciprocal timestamps. *[static]*
  - `0x0010F500`: Initializes rider+0x4D8 (progress origin) from rider+0x4D0 via getter `0x00113128` (through `0x0010F50C`). *[PS2]*
- **`0x0010F560`** - Rider manager refresh run every sixth manager tick (first at game tick 18), at the start of `0x00128AF0`, after the race clock and before any rider controller/physics pass. Runs race ranking `0x0010F998`, pacing bounds (+-50000, writes a +0xEC field) and the pair proximity records: reciprocal planar XY distances and bearings, pair +0x1C set at relationship >= 2, designated peer (`0x0010F878`) at relationship 4. *[PS2]*
- **`0x0010F878`** - Designated peer selection inside the rider manager refresh `0x0010F560` (peer designated at relationship level 4). *[static]*
- **`0x0010F998`** - Race/freestyle ranking, run every sixth tick from `0x0010F560`: key -(remaining + place*20) (20 cm hysteresis), sorted by shell sort `0x003E6328`; rank mode 2 (game-info +0x74) keys on score +0x198, or 0xFFFF0001 when rider +0x878 is set. Detects overtakes and drives the place display `0x0021E1B0`. Slope style ranks 4 riders, slot 1 being a riding computer opponent. *[PS2]*
- **`0x001112B8`** - Motion-mode transition: runs the current motion's exit from table `0x00456B10` (indexed by +0xDE0), stores the new mode, then runs the new motion's enter (e.g. motion-0 entry `0x0013C7A8`, motion-1 enter `0x001399E0`). *[static]*
- **`0x00111408`** - Rider motion dispatcher: jumps through the six-entry table `0x00456B50` on motion owner +0xDE0. Targets (receiver): 0 `0x0013D818` (owner), 1 `0x00139A20` (owner+0x20), 2 `0x00136E98` (owner+0x30), 3 `0x00136958` (owner+0xA0), 4 `0x0013AF28` (owner+0xB0), 5 `0x001391A8` (owner+0x110). *[PS2]*
- **`0x00111538`** - Control-state transition: runs the current control's exit from table `0x00456B90` (indexed by +0xDE4), stores the new control, then runs the new controller's enter (e.g. control-0 entry `0x00131608`); control 13 has no enter, and control 12 enter/exit go through the thunk `0x00111714` and the empty exit `0x00111624`. Second link of the `0x0011FEC8` -> `0x00133128` chain. *[static]*
- **`0x00111578`** - Controller exit dispatch through table `0x00456B90`, used by the control change. *[static]*
  - `0x00111624`: Exit-table target for control 12: empty exit handler (no extra exit work). *[static]*
- **`0x00111630`** - Control-state transition step (third link of the chain) that invokes the new state's entry, e.g. `0x00133128`. *[static]*
  - `0x00111714`: Thunk entry to the control 12 enter `0x00136268`. *[static]*
- **`0x00111728`** - Contains the thunk entry to the control 12 update below. *[static]*
  - `0x0011181C`: Thunk entry to the control 12 update `0x00136508`. *[static]*
- **`0x00111890`** - Builds/resets one rider's FX container (rider+0x77C) by calling the FX component ctors: `0x002DCF28` (+0x3B0 trails), `0x002DAA78` (+0x470 sparks), `0x002E8560` (+0x520), `0x002E6640` (+0x610), `0x002EADC0` (+0x9C0), `0x002EF6A0` (+0xAD0), `0x002D4BE0` (+0xAF0), `0x002E3930` (+0xB00), `0x002DF3B0` (+0xB40), `0x002F1148` (+0xC70 fist), `0x002F64E8` (+0xD20), `0x002C03E8`; then fetches that camera's splash object via `0x002306A8` (viewport vt+0x74). Not the main loop. Also the final FX reset step of the stage teleport `0x00123210` and one of the reset observers alongside `0x0011D660`. *[static]*
- **`0x00111948`** - Updates the rider FX container components. *[static]*
- **`0x00111AA0`** - Snow impact trigger wrapper (rider, pos*, normal*, surface, f12 strength) -> `0x002E23E0(owner+0xB40, ..., kind 0)`; the strength ends up in the snow FX (+0xE0). Crash, scenery, landing and rail-attach visuals all go through it; contact dispatchers `0x0028B180`/`0x00296088` are audio only, and ragdoll body-vs-scenery never calls it. Callers (site): `0x0010EB30` (`0x0010EC04`), `0x00137860` (`0x001379D4`), `0x00137D18` (`0x00138454`), `0x0010E910` (`0x0010EA08`), `0x001242B0` (`0x001245BC`). *[PS2]*
- **`0x00112180`** - Re-attaches a rider's route to the region-row paths of the current path bank; called after `0x0012A340` and by `0x0011D390` as (rider, 1). Looks up `0x0026B5E0(bank, 1, rider+0x86C)` (runtime kind 1 = exported kind 0, index 0; first row when none); row +0x20 is the reset (AI) path, +0x24 the race path. Sets rider +0x4D0 = +0x4D4 = remaining-at-origin minus the `0x0026A638` distance (or remaining-at-origin if smaller) and the route heading +0x4CC; reading another row gives a wrong heading. *[PS2]*
- **`0x00112338`** - Course progress update (with `0x00112FB0`): remaining and best remaining distance, course events and path selection. *[static]*
- **`0x001125C0`** - Rider route progress, run each normal frame for human and computer riders: copies +0x4C4 to +0x4C0, projects onto the path with the ABC cache, writes a 796 cm lookahead into +0x4B0, may change route, then writes the route heading +0x4CC (feeds the next ground forward drive). A route change happens near the path end, after crossing event 18/20, or every 60 ticks when > 500 cm off path; tests actor+0x4D4 best remaining. Computer riders take branch `0x00112A50` and may draw randomness. For the human it runs after course progress; the trick score tick `0x00117C28` (during `0x00121818`) follows it. *[PS2]*
- **`0x001127F0`** - Course (track) path selection: three-candidate bbox order (`0x0026B178`), 796 cm lookahead, 1592 cm cache reset threshold. Separate from the rider route selection `0x00112A50`. *[static]*
- **`0x00112A50`** - Ordinary route selection for human and computer riders (the computer branch of route progress), distinct from the reset selector `0x00112D58` and track selection `0x001127F0`. Gathers candidates via `0x0026AFB8`, `0x0026A428`, `0x0026AC48`, `0x0026AB20`, then updates the ABC cache and +0x4C0/+0x4C4. Computer riders score routes with `0x0010D410` (shared RNG); the human minimizes the sum of two squared geometric distances. *[PS2]*
- **`0x00112D58`** - Forced-reset route selection and refresh over the 129-record AI/reset path table: gathers up to 6 candidates via `0x0026AF98` (requires path field +0x3C; 117 of 129 eligible), projects with `0x0026A428`, may reject beyond pathLength-200 per rider+0x6C0 slot +0x40, and scores squared distances to the projection and to a sample at +796 cm (no velocity lookahead). Checks the old +0xAB8 before installing; invalidates +0xABC and refreshes samples even when unchanged. Differs from the ordinary selector `0x00112A50`. *[static]*
- **`0x00112FB0`** - Course progress/event update (with `0x00112338`), including path-selection rules; when rider +0x4D4 (best remaining) passes an entry of the checkpoint list `0x004D33B8` it calls `0x0010E558` (-> `0x001194C0` for the human in mode 1). *[PS2]*
  - `0x00113014`: Checkpoint crossing walk over the list `0x004D33B8` (to `0x001130A8`). *[PS2]*
- **`0x00113128`** - Getter for rider+0x4D0 (course progress distance). *[static]*
- **`0x00113130`** - Returns a rider's route origin distance (used by the points estimate). *[static]*

## `src/ai/airpredictor.cpp`

Segment `0x00113198`-`0x00113B10`.

- **`0x00113198`** `cAirPredictor_reset` - First half of the air predictor restart `0x001135B8` (followed by `0x00113618`); its writes reinitialise the trajectory state and probably leave the inactive hit_position field untouched. *[static]*
- **`0x00113200`** - Air trajectory prediction step (extension and reverse terrain query), run each airborne tick with `0x00113648`: advances 60 Hz steps until count > 270000/speedLimit or dot(initial unit dir, v) < 0.99*|v|, tracks the apex, expands the chord 2% at each end and queries backwards via `0x0032E100` (kind 1, fraction 1; status 0 -> `0x00336850`, status 2 -> `0x003378C0`). Ranking picks the reverse fraction closest to 1, instances before terrain on ties; a hit also needs the expanded chord to face against the normal. The plain step uses drag 0xBB5A740F and gravity 0xC1FD5556; another branch (possibly sub-tick blend or time bookkeeping) exists but is unconfirmed. *[static]*
- **`0x001135B8`** `cAirPredictor_startLaunchIntoAir` - Full restart of the air trajectory (landing-crash) predictor: calls `0x00113198` then `0x00113618`, clearing times, hit, heading and normal; predictor status drops to 0 for a few ticks. Args (rider+0x788, position, velocity[, speed limit rider+0x2E4]). Called by the air entry `0x001399E0`, by `0x00105D98` in crash air (`0x00105EA0`..`0x00105EEC`), by `0x001057B8`, and to reseed after a rider-pair velocity change. *[PS2]*
- **`0x00113618`** `cAirPredictor_initLaunch__FPcP5sQuadT1` - Predictor reseed and second half of the restart `0x001135B8` (clears hit/heading/normal); called alone it keeps the elapsed time. *[PS2]*
- **`0x00113648`** - Adaptive airborne trajectory updater, called each airborne tick from the air motion dispatch `0x00139A20` (with `0x00113200`) and seeded on charged and passive air entry. Lazily extends the prediction, reseeds a status-1 prediction more than 0.2 s behind elapsed time (copies current motion into both state pairs, prediction time = elapsed), and advances the rider's actual air motion via the 60 Hz step `0x001139A0` (capped at 3333.3335 cm/s from the first air tick). Status 1 = valid prediction (camera gates on 1/3); beyond 60 s it switches to status 2, then 3. Fixed-step cache, interpolation only when the remainder > 0.01 s; world query modes 0/2. *[PS2]*
- **`0x001139A0`** - 60 Hz air trajectory integration step (Z-up, cm): p += v*dt first, horizontal components += v*drag, vz += the rise increment if vz > 0 else the fall increment, then |v| is capped to trajectory+0xA8 (3333.333496 cm/s observed). Constants: dt 0x3C888889 (1/60), drag 0xBB5A740F (0.2/s), rise 0xC162AAAB (8.5 m/s^2), fall 0xC1FD5556 (19 m/s^2); float ops round toward zero. *[PS2]*
  - `0x00113A74`: Speed-cap scalar divide (nearest rounding). *[static]*

## `src/ai/ridermetrix.cpp`

Segment `0x001172E0`-`0x0011B698`.

- **`0x00117540`** - Score object run reset; called first in the arrival placement of `0x0011D390`. *[PS2]*
- **`0x001175F8`** - Combo reset, called by the score reset `0x00119368`. *[static]*
- **`0x00117638`** - Adds a trick to the combo; the multiplier becomes clamp((count+10)*0.05, 0.5, 2). *[PS2]*
- **`0x001176F8`** - Suspends the grounded combo-expiry timer by writing -1 to comboTimeout (score object +0xA4); does not clear the hold threshold index or completed durations. *[static]*
- **`0x00117718`** - Combo payout, called from the per-tick score update `0x00117C28` when the combo clock runs out. *[PS2]*
- **`0x00117838`** - Per-trick score state reset/commit boundary: resets the grab/trick score fields (rail distance +0x24 and manual timer +0x2C default to -1) but keeps the combo timeout +0xA4 and multiplier +0x1C4. Used on landing (with `0x00119D40`), by the score reset `0x00119368`, board press end `0x00119A38` and Uber score end `0x00119958`. *[static]*
- **`0x00117908`** - Pays the inverted/stall accumulator (+0x1C) at landing: converts +0x1C * 10000 into points rounded to a multiple of 10. *[PS2]*
- **`0x00117948`** - Pending trick points getter: trunc(multiplier*accumulated*10000 + 5), then drops the remainder mod 10 (uses rider+0x1C4 and score +0x14, signed integer truncation; bonus points +0x84 are separate). Also referenced as the crash boost-loss test (penalty -0.25), a role that is unconfirmed. *[static]*
- **`0x00117990`** - Double-precision rounding helper used in the trick landing award. *[static]*
- **`0x001179E0`** - Removes a HUD bank slot / clears score notification types; used by threshold bonuses together with `0x00117B88` (clear event types 28..32, then request 32 grab hold or 29 rail distance). *[static]*
- **`0x00117A58`** - Allocates a HUD bank slot for message types 0x23 and above; types below 0x22 own slot [type]. *[PS2]*
- **`0x00117B88`** - Posts a score HUD popup/notification entry (type, value, duration), e.g. the race/checkpoint bonus type 0x29 with the value and 2.5 s, or event 32 (grab hold) / 29 (rail distance) after `0x001179E0` clears types 28..32. *[PS2]*
- **`0x00117C28`** - Score object per-tick update: counts down the combo clock +0xA4 on the ground and pays out via `0x00117718` when it runs out; accrues air, hold, rail and +0x2C; calls thresholds `0x00119210` and the HUD update `0x00117FE0` (landing messages, spin dial). Frame order: HUD draw `0x001EC3F8`, HUD update `0x001EA930`, provider `0x00127998`, rider post `0x00121750`, then this, during the per-actor phase `0x00121818` after route progress `0x001125C0`. Also runs for the slope-style opponent's score object. *[PS2]*
  - `0x00117D24`: Held-grab tick branch (to `0x00117D70`): if +0x40 >= 0 advances the hold timer by timeScale/60; accumulated +0x14 adds the hold increment +0x3C once per tick without time scaling. *[static]*
  - `0x00117D74`: Rail distance scoring block (to `0x00117DFC`): +0x24 += speed*scaled 1/60; +0x14 += distance*5e-5; if board-up Z < 0, +0x1C += distance*2e-4; at most one threshold from `0x00459F68` per tick, then event 29. *[static]*
  - `0x00117E00`: End of the rail scoring stage, before the manual/Uber stages (paired with `0x00117E04`). *[static]*
  - `0x00117E04`: Second boundary at the end of the rail scoring stage (with `0x00117E00`). *[static]*
  - `0x00117E58`: Active-time accrual (to `0x00117E84`): if activeSeconds >= 0, adds dt to it and dt*0.05 to accumulated points; a negative timer exits to `0x00117E8C`. *[static]*
- **`0x00117FE0`** - Score HUD update, called by `0x00117C28`: writes the spin dial slot 0xD, direction slots 0x10-0x17, landing messages slot 0xE (+0x1C inverted points), boost widgets, total slot 7, combo slot 3 (+70/tick display) and slots 2/0x19/4; also triggers the points tick, pending Uber and Tricky start/end sounds. Sub-blocks: stall bonus `0x001185F0`, boost fill `0x001188F8`, SUPER UBER letters `0x00118A28`, pending letters `0x00118AF8`, Tricky flash `0x00118C2C`. *[PS2]*
  - `0x001185F0`: Stall bonus block (to `0x001188CC`): STALLED! in slot 0xE and +0x1C accrual; in air control 5, input > 157.5 deg opposite the rotation adds b/1200 per tick (b = 1 spin axis, 5 flip axis, 2 diagonals); paid at landing by `0x00117908`. *[PS2]*
  - `0x001188F8`: Boost fill HUD block (to `0x00118A24`): preview slot 5 = min(meter + pending reward, 1) (no pending in drain mode 3) and stored-meter slot 6 approaching the raw meter, via `0x001171A8`; step 1/60 with strict compares. *[static]*
  - `0x00118A28`: SUPER UBER letter slot 8 update (to `0x00118AF4`): tier 0 removes the slot via `0x001179E0`; else count = min(tier,10)-1, target = min(count/9, 1), approached by gp-0x7A64 (0x3BDA740F); count passed in a3 to `0x001171A8`. *[static]*
  - `0x00118A44`: Tier-zero branch of the letter update (delay-slot load at `0x00118A48`, jumps to `0x00118AFC`). *[static]*
  - `0x00118AF8`: Pending-Uber letter slot 10 update (to `0x00118C28`): remaining = 10 - min(tier,10); count = min(+0x54 + (+0x5C ? 1 : 0), remaining), target min(count/9, 1); slot removed (hide `0x0029B3C0`) if no Uber or remaining <= 0. *[static]*
  - `0x00118C2C`: Tricky flash slot 9 update (to `0x00118D4C`): appears only if rider +0x2F0 > 0 (`0x00299638`); removed when old fraction == 1 and timer == 0 (`0x002997B8`); else target = 1 - timer*0.05, step 1/60, palette tier = min(tier, 11). *[static]*
- **`0x00118FF8`** - Writes the committed trick name into HUD slot 0. *[PS2]*
- **`0x00119068`** - Three-entry trick history insertion: appends the ID into the first empty entry unless the immediately preceding entry already holds it; a full history is left unchanged. Called first by the trick score end `0x001197D8`. *[static]*
- **`0x001190F0`** - Trick repeat-history update for repeat penalties: compares the trick identity against a 10-entry ring (80 bytes at score +0xA8) and overwrites the slot at cursor +0xF8 (mod 10); returns the repeat count (saturates at 10). Nonzero scoring fields +0x08/+0x20/+0x70/+0x7C/+0x28 skip both matching and insertion; (word1 & 0x3F800) == 0x800 or (word0 & 0x0FC00000) == 0x00400000 skips matching but inserts. A repeat posts popup 0x24 and divides the points (a repeated Monster is halved). Dependency of the landing commit `0x0011A228`. *[PS2]*
- **`0x00119210`** - Score threshold update (indices +0x88..+0x98), called from `0x00117C28`. The grab hold-time bonus checks only the current authored threshold (3 s -> 4000, 6 -> 2000, 9 -> 1000, -1 sentinel), one bonus per crossing: +0x8C++, bonus = trunc(points*multiplier(rider+0x1C4) + 0.5) added to +0x84, clears events 28..32 then requests event 32 (1.5 s) via `0x001179E0`/`0x00117B88`. *[PS2]*
- **`0x00119368`** - Rider score reset (score, reason) for placement/reset: banks lost points (`0x0011A7A8`) into +0x1A4, resets the trick score (`0x00117838`) and combo (`0x001175F8`), increments the reset count +0x120. Returns the boost penalty meter * -0.7 for manual reason 0 (gp-0x7A28) or * -0.1 for a nonzero reason; `0x0012F398` passes it to `0x0010E098`, while the stage teleport discards it. *[PS2]*
- **`0x001193E0`** - Score object finish: pays the open combo into the total +0x198; reached via the finish routine `0x00125108`. *[PS2]*
- **`0x00119400`** - Knockout score hook: +1 KO (+0x128) and popup 0x2C (1.5 s); called from the crash attack `0x0010E468` alongside its meter award. *[PS2]*
- **`0x00119448`** - Score multiplier pickup (from `0x0010E830`): +0x130++; if the HUD bank is present and +0x18 < value, sets HUD slot 4 = `0x001171A8(type 4, int(value), 0, 0)` and +0x18 = value; then calls `0x0010E098(rider, 4, 0)` and the pickup sound `0x0029CED8(audio, 2, rider, value)`. *[PS2]*
- **`0x001194C0`** - Slope-style race/checkpoint trick bonus (human only), reached via `0x00112FB0` -> `0x0010E558` when best remaining +0x4D4 passes a `0x004D33B8` checkpoint; off if bit 9 of *`0x005308D0` is set or game mode != 1. Requires rider+0x6C0 vfunc +0x44 and `0x00238510(G+0xC0, rider id, checkpoint)` to pass; posts HUD type 0x29 (value, 2.5 s) via `0x00117B88` and Arcade_Bonus speech (0x20A9 via `0x002A3CE8`, its only source). Also shows the finish-line trick bonus and the '+60' checkpoint popup; handler ids 0/1/2/5/6 pay it, and freestyle kind 1 accept `0x002398E8` adds value x 60 ticks to the limit. *[PS2]*
- **`0x00119608`** - Score point pickup: adds the point icon value (2000/3000/5000/10000) to the score; chain stage builtin27 type 6 -> `0x0010F1C0` -> `0x0010E8B8` -> here. *[PS2]*
- **`0x00119708`** - Trick score begin (ordinary and mapped tweak/Uber begins): +0x14 += beginPoints*1e-4 (0x38D1B717), +0x3C = holdPoints*0x35DFB23B, +0x40 set to 0 only if negative; score IDs >= 35 set activeUber +0x5C. Returns exactly +0.0, so the following `0x0010E098` call changes no boost. Paired with the end `0x001197D8` in the grab lifecycle. *[static]*
- **`0x001197D8`** - Trick score end: first inserts into the history (`0x00119068`), then bumps category counters (ID < 19 -> +0x4C, 19..34 -> +0x50, >= 35 -> +0x54, plus +0x58 if boost tier +0x2F4 >= 6, signed compare); +0x44 += +0x40, updates longest +0x48, clears +0x5C and sets +0x40 = -1. Keeps the increment and combo timeout; returns +0.0 (no boost through `0x0010E098`). *[static]*
- **`0x00119898`** - Rotation score helper (spin/flip replacement arithmetic) used by the trick commit `0x0011A228`, paired with `0x001198D8`; also used with `0x00119AD8` for board pivots and from the rail rotation score `0x00119918`. *[static]*
- **`0x001198D8`** - Rotation score update (replacement arithmetic) used by the trick commit, paired with `0x00119898`. *[static]*
- **`0x00119918`** - Rail spin score (rider+0x790, style, spin): calls `0x00119898` to replace the prior absolute spin contribution in the pending reward with the new one and updates style +0x20; its immediate boost return (0) is passed to `0x0010E098(rider, 1, value)`. *[static]*
- **`0x00119938`** - Rail Uber score begin (rider+0x790, tier, style): initialises activeSeconds +0x6C, active +0x70, activeUber +0x5C and the styles without resetting points; the result goes to `0x0010E098`. *[static]*
- **`0x00119958`** - Rail Uber score end (rider+0x790, style): increments +0x54 and +0x74, commits via `0x0011A228`, resets through `0x00117838`, then restores +0x24 distance, +0x30 airSeconds and the new style; returns the Uber award, which is forwarded to the meter award `0x0010E098`. *[static]*
- **`0x001199F8`** - Score routine for a board press reaching full depth. *[static]*
- **`0x00119A38`** - Score routine for a board press end; calls the per-trick reset `0x00117838`. *[static]*
- **`0x00119AD8`** - Score routine for a board pivot (used together with `0x00119898`). *[static]*
- **`0x00119B08`** - Score bail/crash penalty: lost-point popups, crash counters and statistics, score reset, and a -0.25 meter penalty through `0x0010E098` when points were pending. An Uber bail ends Tricky (SFX 0x69) and stops the pending-Uber sound. Called from the hard crash entry `0x0010EB30`. *[PS2]*
- **`0x00119BB0`** - Quick recovery score hook, reached via `0x0010F280`: +0.1 meter and popup 0x21 RECOVERED!. *[PS2]*
- **`0x00119BF0`** - Handplant score begin. *[static]*
- **`0x00119C38`** - Handplant score launch commit. *[static]*
- **`0x00119C98`** - Score hook for a new D-pad trick started in the air (score object trick start); called from air control `0x00133734` phase 3. *[PS2]*
- **`0x00119D40`** - Trick score commit boundary (scoring object rider+0x790, stance, alternate, style, flag), used on landing via `0x0010E910` and on ground/air rail entry (rail attach passes (rider+0x790, +0x320 != +0x324, 0, style, +0x330)). Commits the current trick via `0x0011A228` (forwarding the five args), resets per-trick fields via `0x00117838`, then reseeds stance/+0x04/style/flag +0x10, starts +0x24 and +0x2C when applicable and restores +0x70, +0x5C, +0x6C; timeout +0xA4 and rider+0x1C4 survive. On landing, stance = (rider+0x320 ^ +0x324) != 0, alternate = normal.z < 0.7, flag = rider+0x330; a nonzero incoming style first adds 0.13 to a positive pending reward. The caller passes the returned value to `0x0010E098(rider, 1, value)`. *[PS2]*
- **`0x00119E38`** - Score object takeoff boundary: ordinary commit/reset that keeps an active Uber; used for takeoffs, rail explicit jump and passive rail loss, and as the second side-effect call of the land-on-top path `0x001057B8`. Reached via `0x00114298`, whose ramp flag (branch at `0x00114660`) arrives in identity field +0x4 as the alternate flag instead of a normal test. *[PS2]*
- **`0x00119EF8`** - Career/score award hook by kind: kind 3 = collectible cash (calls `0x001E3760` before adding the cash), kind 4 on Big Challenge completion (with the popup); also called at trick commit. *[static]*
- **`0x0011A088`** `cRiderMetrix_evAutoResetSurface__FPv` - Posts the 'Wrong Way!' HUD message (type/slot 0x33, 1.5 s), paired with the `0x001210B0` timer resets; reset handling for reasons 1 and 4. *[PS2]*
- **`0x0011A168`** - Hook for a new D-pad trick in the air that runs monster/named trick detection (`0x0011B1A8`) and calls the monster-trick speech `0x0029B7E0` only for a monster (bonus > 0). *[PS2]*
- **`0x0011A228`** - Trick landing commit (five args forwarded by `0x00119D40`): builds the identity (`0x0011A8C8`), runs named/monster detection (`0x0011B1A8`; speech `0x0029B7E0` only for bonus > 0), checks repeat history (`0x001190F0`) and divides the reward +0x14 by (repeats+1), adds to the combo (`0x00117638`), updates the total +0x198, posts popups 0x23/0x24/0x18, writes name slot 0 (`0x00118FF8`) and calls the career award `0x00119EF8`. Rotation scoring via `0x00119898`/`0x001198D8` uses the air controller's scored +0x30/+0x34; a positive-score gate controls the Uber count commit. Rail commits add a separately rounded inverted-points bonus that does not change the boost reward. Returns at once when every human has finished (`0x0012A250`). *[PS2]*
  - `0x0011A440`: Repeat division block: divides the accumulated trick reward (+0x14) by (repeat count + 1), giving the boost meter delta. *[static]*
  - `0x0011A458`: Inverted-rail points block: adds inverted-rail points after the repeat division, with no trick/rider multiplier and no meter increase. *[static]*
- **`0x0011A7A8`** - Computes and posts the points lost on a reset placement; `0x00119368` banks the return into +0x1A4. *[PS2]*
- **`0x0011A8C8`** - Trick identity builder: produces the 8-byte packed identity (two words holding direction, spin, flip kind, flip count, flip spin, grab and late grab) and a validity return, adjusting the spin (+0x34) / flip (+0x38) signs in place. Uses the 319-byte rotation tables `0x0043D388`/`0x0043D4C8` and two degree scales, normalizes rotations by signed 180-degree buckets, with a fallback record for out-of-table rotations. The identity is formatted into text by `0x00116950`. *[PS2]*
- **`0x0011B1A8`** - Named/monster trick detection: finds the first of 24 rows at `0x0043D608` whose seven identity fields (direction, spin, flip kind/count/spin, grab, late grab) all match; sets the identity to {0, id<<27} (named id in word1 bits 27..31), adds bonus*0.0001 to +0x14, posts slot 0x32 (1.5 s) and returns the authored bonus. Runs at every commit (`0x0011A228`) and new air D-pad trick (`0x0011A168`); the monster speech `0x0029B7E0` (Arcade_Uber 8) fires only when the bonus > 0. No unlock check; a late flip joins the same identity (no monster). *[PS2]*
- **`0x0011B3F8`** - Frame-begin dynamic maximum speed (rider +0x2E4), the top speed stat consumer: interpolates standing/crouched limits from the 48-float table `0x004A6310` using the normalised top-speed stat, surface factor, charge/boost and stance, converts km/h to cm/s, with retention 0.9 when increasing and 0.97 when decreasing. Uses the real motion mode (crash 2, reset 3); only motion 1 (air) is special-cased to 3333.33, and motion 5 uses terminal 1 and crouch 1. Follows the crouch in control 3. *[PS2]*

## `src/ai/rider.cpp`

Segment `0x0011B698`-`0x001286A0`.

- **`0x0011BBE8`** - Collects the equipped BOLT entries of the base rider for the race model; a cheat skin (setup slot +0x12) collects its whole bucket. *[PS2]*
- **`0x0011BE88`** - Resolves the equipped entries' textures for the race model, using the '$' rule of `0x0014B988`. *[PS2]*
- **`0x0011C138`** - Adds each collected entry's four LOD models by name at its part slot (`0x0030D8B8`); the first model of a file id makes the part. *[PS2]*
- **`0x0011C298`** - Rider model init/load step (called from `0x001296F8`): computes the channel-1 reaction bone masks via `0x00310CE8`: +0x8C0 from bone list `0x00457A90`, +0x8C8 from `0x00457B38`, +0x8D0 from `0x00457BC8` / 'morph' (`0x004A1090`), ORed into both. Values: Zoe/Sam/Griff 0x8000FFFE/0x8000FFF8, Psymon/Luther 0x4000..., Allegra/Moby 0x2_0000...; +0x8D0 = 0x870 for all. Also hides NIS part slots and sets secondary motion enables. *[PS2]*
  - `0x0011C61C`: Part-hiding block of the rider init: hides part slots 5, 6, 8, 9 and 11 (NIS head, eyes, NIS hands, NIS right hand, PDA). *[PS2]*
  - `0x0011CF70`: Secondary motion enables (to `0x0011D1B8`), tested per part with `0x0030EBC0`: channel 0 = any part in slots 0x0F..0x1D except 0x12; channel 1 = 0x1E/0x1F/0x20/0x22; channel 2 = 0x24/0x26/0x27/0x2A. *[PS2]*
- **`0x0011D390`** - Places the rider at a region on arrival (course, station or backcountry; event kinds 4..6), shared with the reset control and start-grid control 6: score reset, `0x00117540`, route re-attach `0x00112180(rider, 1)`, then `0x0011DE60` at the entry row (courses < 14 -> (rider, 1, 2), session point 1; backcountry -> (0, 1), grid slot 0; stations -> (0, 2)). Then `0x00119368(score, 1)`, control 0, motion 0, `0x0011D660` (semantic 5), `0x0011DF18`; the tail zeroes rider +0x2E8..+0x2F4 while meter +0x2F8, amount +0x2FC and retained +0x2E4 carry over. A transport arrives from the loop's limbo state (+0x370 = +0x380 = (0,0,1), surface 0). *[PS2]*
- **`0x0011D660`** - Rider reset placement (rider, &P, &D, semantic, clearance) with a camera cut: one vertical world probe from P-7000 to P+200 cm (world Z, flags 2, preferred fraction 0.9791666865348816, query `0x00336850`; a miss keeps the point and world-up), then adds the clearance (negative skips probe and offset); heading from D.xy and physical frame via `0x0011E098`. Not an iterative safe-point search: the 200/1000 cm argument is vertical clearance (Snow Jam variant 0 uses 200). The teleport uses semantic 5 (RNORM_FWD_CYC) and clearance 0; resets place on reset tick 21. The hair `0x00120378` selects from the pose built here and in `0x0011EB98`. *[PS2]*
  - `0x0011DAD0`: Start of the placement tail: zeroes velocity/contacts, the 19 filter triplets 0x1F0..0x2D0 (+0x25C set to 1) and spin/flip/boost/style/manual fields, restores +0x320 from +0x324, plays clip 287, then `0x0011EB60(1)`, `0x0011EB98`, `0x0011E150`. *[static]*
- **`0x0011DE60`** - Region placement (rider, index, kind): finds the region row via `0x0026B5E0(bank 0x004D33A0, kind, index)` (runtime kind = exported kind + 1; the bank's first row if none matches), sets control 0 and motion 0, calls `0x00119368(score, 1)`, then `0x0011D660` (semantic 5, clearance 0) and `0x0011DF18`. Used for arrivals (`0x0011D390`), session points (state 15: (rider, k+1, 2); (rider, 1, 2) = session point 1 on free-ride return), and peak-run and rival rolling starts ((rider, 0, 1)). The motion-0 entry `0x0013C7A8` runs before `0x0011D660`, so +0x390 keeps the old +0x370. *[PS2]*
- **`0x0011DF18`** - Final placement step: freezes the animator (rate 0) and sets the exit velocity to physical forward (+0x1B0) x 833.333374 cm/s with z = 0 (stores zero to +0x1E8; chop rounding). Called after `0x0011D660` by region placement and by the reset control after it requests clip 287; the stage teleport `0x00123210` does not call it and keeps the slope z component. *[PS2]*
- **`0x0011DFE0`** - Rotates the rider's physical quaternion about a world axis by an angle: pre-multiplies (axis*sin(a/2), cos(a/2)) without normalising (round-toward-zero arithmetic); the caller then rebuilds the basis via `0x0011E098`. Used by stance restore/alignment for styles 3/4 (+/-pi/2, 0x3FC90FDB / 0xBFC90FDB), heading changes and the crash prewind compensation before the presented-root bake. *[static]*
- **`0x0011E098`** `cRider_updateOrientationImplicit` - Normalises the rider's physical quaternion and rebuilds the orientation matrix columns right/forward/up at +0x1A0/+0x1B0/+0x1C0 (VU operation order). Called after quaternion changes (`0x0011DFE0`, stance alignment, `0x0013AD20`, `0x00134CB0`, air stance switch, the `0x001065B0` heading correction, rail contact response), at `0x0013F11C`, by `0x0013C140`, motion-3 update `0x00136958`, reset placement and control-5 exit. *[static]*
- **`0x0011E150`** - Rebuilds the rider's query bounds (not the posed bones): an 8-corner box from physical right/forward/up (+/-150 cm forward, +/-100 cm sideways, +250/-50 cm up, source order) around rider+0x110 unless an override is passed, plus a query sphere (centre, radius 250); 12 output words at +0x400/+0x410, W lanes keep position W, extents independent of body scale. First call of the rail motion post stage `0x0013BFA8`; also run in the placement tail. *[PS2]*
- **`0x0011EB60`** - Rider local pose preparation (first staged pose call, via `0x00121700`): advances/samples the animation, then processes completion events via `0x003123C0`/`0x00312598` and `0x00312490`; runs before `0x0011EB98` and the second motion/contact phase. Called as (rider, 1) for a full-rate step in the placement tail of `0x0011D660`. *[static]*
- **`0x0011EB98`** - Rider world pose stage (second staged call via `0x00121728`, before the second motion/contact phase): runs FK/IK from the physical root (+0x110/+0x120; optional second root +0x130/+0x140 when +0x150 is nonzero) with board correction and the +0x2C8 body lift (body root only), leg IK `0x0011F3D8`, and builds the pose-time body spheres (`0x00106828`). In air control 5 it first calls `0x00134DD0` to form the rendered pose. Also builds the placement pose for reset placement `0x0011D660`. *[PS2]*
  - `0x0011ED80`: Board correction block (to `0x0011F148`): aligns the board to the filtered desired normal +0x390 with weight +0x2BC and lift +0x31C (contact normal +0x370), then rebuilds the board child. *[static]*
- **`0x0011F3D8`** - Two-link leg IK solve (thigh/shin/foot) using bindings at rider+0x8E0/+0x910; called from the world pose `0x0011EB98`. *[static]*
- **`0x0011FA10`** `cRider_doLeanPoseAdjust` - Presented-root bake (rider, root): cancels lean against twice the brake magnitude, rotates about the board-local pivot, applies the presentation roll (triplet +0x250) and a 45 cm*turn*bodyScale lateral offset along rider+0x3B0; no `0x0011EB98` lift. Used by rail/motion-4 entry `0x0013AD20`, the crash entry `0x0010EB30` and the air root. *[PS2]*
- **`0x0011FE78`** - Requests a rider motion mode (rider, motion): forwards to `0x001112B8(rider+0x77C, motion)`, which runs the old motion's exit and the new enter immediately, e.g. (rider, 1) enters air on a terrain departure (its gain-focus handler runs); the crash entry uses 2, running the ground exit `0x0013F410`. *[static]*
- **`0x0011FE98`** - Returns the rider motion mode = motion owner +0xDE0 (1 air, 2 crash, 3 reset, 4 rail, 5 handplant). Used to tell true air from handplant, rail and reset-hold for the long-air reset timer `0x001210B0`, to gate passive departures (proceed on 0), by big-air logic, board loop volume (0 in crash), BodySnow (FX+0xD4), camera input and the Uber update `0x00136508`. *[PS2]*
- **`0x0011FEC8`** - Requests a rider control state (rider, control): forwards to `0x00111538`, which runs the transition immediately (chain -> `0x00111630` -> the new state's entry, e.g. `0x00133128` on jump release). The crouch request `0x001162C8` calls it with control 2 at `0x00116344`; the soft-collision (control 3) landing exit from `0x00139C88` uses it with 0, ending the soft controller and clearing the jump latch while retaining rider+0x2DC and the prewind triplets. *[PS2]*
- **`0x0011FEE8`** - Returns the rider control state: *(*(obj+0x18)+0x77C)+0xDE4, i.e. motion owner +0xDE4. *[static]*
- **`0x0011FF48`** - Returns the world row of the rider's cached posed head/primary bone (rider+0x89C) with w = 1; used as camera target slot 0x08 and as the point for rider lighting assembly and light selection. Reads cached posed geometry; it does not re-pose. *[PS2]*
- **`0x0011FF98`** `cRider_getMass` - Effective rider collision weight for the rider-pair inputs: CHARDB integer weight (`0x00530970` + characterId*0x88 + 0x40) x (1 + toughness stat (`0x00148F50`) x 1.5003352165222168) x (1 + rider+0x2FC x 10). *[static]*
- **`0x00120038`** - Grab playback speed (tricks stat consumer): 1 + grabStat*0.29988324642181396. *[static]*
- **`0x00120090`** - Approaches the rider time scale +0x300 toward its target (rubber-band pacing, with `0x0010DEF0`); published before control handling. *[PS2]*
- **`0x001200D0`** - Boost/meter timer stage, run after controller dispatch (and after `0x00115B58`) and before the common smoothing, alongside boost control `0x00114130`: window/modifier decay, super timer, stored meter, active drain (may briefly take the meter below zero by less than one drain step; not clamped) and meter-policy decay. When the Tricky/Super timer ends, tier 10 drops to 5 with 20 s and reaction kind 6 is requested via `0x0010E028` (for every rider). The virtual `0x00140BC8` getter result is unused. *[PS2]*
- **`0x00120378`** - Hair secondary motion update, run after the controller: per-tick rate on channels 3..5 = clamp(|v - surfaceV|/1666.67, 0.5, 2) from pre-motion velocity; when the `0x001298C8` tick % 6 == rider+0x86C it picks one of seven SH_* semantics from the wind (cached bone +0x8A8 times +0x950/+0x960): still <= 138.9 cm/s, up/down |z| > 888.9, side |x| > 416.7, else fore/aft. Selects from the pose built by reset placement `0x0011D660` -> `0x0011EB98`. *[PS2]*
- **`0x00120D90`** - Leg IK weight update (+0x318): approaches 0 for main animation classes 20..26, otherwise 1, with step ~0.05 (0x3D4CCCCE) and exact snapping thresholds, once per tick. Called by the timer phase `0x001210B0` after all `0x00121068` controller updates and before `0x001211F8` smoothing, so it sees the newly requested main class. Separate from `0x001352A8`. *[static]*
- **`0x00120E30`** - Start of the rider controller and physics pass functions (range `0x00120E30`..`0x001217F8`) run by the rider manager; audio RNG draws (`0x002ADF60`, `0x002A4CA0`) happen during these passes. *[static]*
- **`0x00120E50`** - Rider light refresh: rebuilds the rider query scope (+0x860) from the query bounds +0x400/+0x410 with `0x00332DB8`, then runs light selection `0x00122088`. No local tick gate (timing comes from the `0x0026DBF0` job caller); called by `0x0012B788` for each primary rider. *[PS2]*
- **`0x00120E88`** - Per-tick rider FX update, the last entry in the rider manager's FX pass list; calls the bone-emitter controller update `0x002F1A08`. *[static]*
- **`0x00120F20`** - Per-actor world-update phase 1 of 12 in `0x00128AF0` (after the manager, before providers): rider query update that clears rider+0x3FC (alternate irradiance bank trigger). *[PS2]*
  - `0x00121020`: Motion-tick block that clears the rider +0x9D0 contact offset on the next motion tick. *[static]*
  - `0x0012102C`: Increments the rider +0x470 finish timer by 1/60 (to `0x0012104C`). *[static]*
- **`0x00121068`** - Per-actor controller phase (2 of 12 in `0x00128AF0`): runs the rider's providers and active controller (stage-1 RNG draws). Animation selectors (ground block `0x00131878`, air block `0x0013437C`, passive air) run on this tick, before timers, `0x001211F8` filtering and pose sampling. *[PS2]*
- **`0x001210B0`** - Per-actor timer phase (3 of 12), after controller dispatch and before `0x001211F8`: decays collision history rider+0x3E0 by 0.980000019, +0x3F0 by 0.955999970 and a secondary counter by 0.978333354, calls the leg IK weight update `0x00120D90`, and requests a reset `0x00116120(rider, 0, 2)` (reason 2, out of bounds) when +0x3F0 > 4.5 (direction changes), or in air (motion 1, or motion 2 submode 1) when a live prediction (status 1/3) has > 45 s left or the bounce counter +0x3F4 > 5. A reset request enters motion 3 immediately; a re-request restarts the progress. Only true air (`0x0011FE98` == 1) counts; its timer resets go with the 'Wrong Way!' message `0x0011A088`. *[PS2]*
- **`0x001211F8`** - Shared control smoothing pass (phase 4 of 12): once per tick in every motion mode, moves each control triplet (current, rate, target) toward its target without overshoot: +0x1F0, +0x1FC, +0x208, +0x214, +0x22C, +0x238, +0x25C (rail tolerance, approached even off the rail), +0x268, +0x274, +0x280, +0x28C, +0x298, +0x2BC, +0x2C8, the prewind pair and presentation filters. Runs after the controllers, boost timers and `0x00120D90`, before the motion update; still runs on the deferred first tick of passive-air entry, so charged-release ground-leave targets must be set before it. The ground selector sees the old filtered values; the air-adjust driver `0x001043F8` reads filtered +0x28C/+0x298 after it. *[PS2]*
  - `0x0012130C`: Triplet approach step (to `0x00121348`): moves current by rate toward target and snaps without overshoot. *[static]*
- **`0x001216E0`** - Per-rider motion update (per-actor world-update phase 5 of 12 in `0x00128AF0`). *[PS2]*
- **`0x00121700`** - Per-actor phase 6 of 12 in `0x00128AF0`: local pose preparation stage that calls `0x0011EB60` (followed by `0x00121728`). *[PS2]*
- **`0x00121728`** - Per-actor phase 7 of 12 in `0x00128AF0`: world FK/IK pose stage that calls `0x0011EB98` (after `0x00121700`). *[PS2]*
- **`0x00121750`** - Rider post / second motion phase (8 of 12 in `0x00128AF0`): touchdown and queries (`0x0013F488`/`0x0013AA48`, `0x00105398`), rider-pair dispatch `0x00107888`, the `0x0013F358` clamp, rail and crash posts; landing commits happen here. Then commits the accumulated contact displacement rider+0x9D0 to the cached rendered bones and head via `0x00310530`. In the frame it runs after the HUD update and provider, before `0x00117C28`. *[PS2]*
- **`0x001217F8`** - Per-rider course progress step (phase 9 of 12 in `0x00128AF0`), with `0x00121818`. *[PS2]*
- **`0x00121818`** - Per-actor phase 10 of 12 in `0x00128AF0`: calls course progress `0x00112338`, then route progress `0x001125C0`, runs the trick score tick `0x00117C28` and the time-limit check `0x00125228`, and dispatches the selected-instance contact: when rider+0xA30 (stored by `0x00105398`) is set, it runs the entity contact (vtable +0x144) or, without an entity, the instance's authored slot-2 stage program via `0x0030A060` (flag-2 trigger volumes, course-script spline launches). Drives pickup collection and crashbag RollerModifier creation; same path for CPU riders. Sometimes called the last/race-end pass, but `0x001218D0` and `0x00121950` follow it. *[PS2]*
  - `0x00121840`: Entity path (to `0x00121858`): calls the entity contact interface (vtable +0x144) with rider+0xA60, rider+0x9E0 and rider+0x6C0. *[static]*
  - `0x00121864`: No-entity path (to `0x00121870`): calls the slot-2 program runner `0x0030A060` directly. *[static]*
- **`0x001218D0`** - Per-actor phase 11 of 12 in `0x00128AF0` (after the contact pass `0x00121818`): rider environment/painter sampling with its own painter instances (separate from the camera path); sets the patch location id rider+0x434 via `0x0022E0E0` from the +0x430 track byte. *[PS2]*
  - `0x00121928`: Environment sample block: supplies the environment index (`0x00140B80`), rider+0x460/+0x464 coordinates and the -99999 sentinel. *[static]*
- **`0x00121950`** - Per-actor phase 12 of 12 in `0x00128AF0`, run after `0x001218D0` and before the FX passes; calls the audio painter query `0x002898A8`. *[static]*
- **`0x00121AA0`** - Orientation approach (rider, targetUp, targetForward, gain, limit) toward a target frame: up alignment `0x0031BB30`, optional heading correction, shortest-sign quaternion delta, gain and capped angular rate, final normalisation. Used for airborne auto-alignment toward the predicted landing surface (with the `0x00139A64` stage), on pinned handplant ticks, and by the rail update (limit 1e10). *[static]*
- **`0x00122088`** - Rider local-light selection: takes the posed primary bone (`0x0011FF48`) and calls `0x002F5B68` on the rider+0x860 scope list (+0x210/+0x214) to select up to 8 lights. Its cadence comes from job gating (`0x0026CC48`/`0x0026CD20`/`0x0026F850`), not a local gate (calls seen every 3 ticks in one run, not proven fixed). Also run in the placement tail of `0x0011D660`. *[static]*
  - `0x001220C4`: Epilogue of the light selection (point after selection completes). *[static]*
- **`0x001220D8`** - Rider irradiance/lighting assembly: builds the 160-byte (40-word) coefficient bank at rider+0x7C0. Order: copy the environment bank from `0x002EDFB8`, posed primary bone via `0x0011FF48`, rim scalar `0x002EEFA8`, rim prep `0x00389CB8`, the 8 local-light pointers at rider+0x794 via `0x0038A6A8` in order (non-null kind 0/3 entries ignored), then if the RFX (rider+0x77C) exists its +0xD30 block adds ambient (`0x00389558`) and counted directional pairs (`0x00389520`/`0x00389308`). Called once per tick for the human rider; at draw time it sees the live camera matrix at renderer stack depth 0. *[PS2]*
- **`0x00122278`** - Rider visibility test, probably including an occluder part (not recovered). *[unconfirmed]*
- **`0x00122448`** - Rider draw; contains the renderer coefficient-setter lookup below. *[static]*
  - `0x001224C8`: Rider draw block (to `0x001224E4`): resolves renderer vtable slot 0x228/0x22C (coefficient setter `0x003954D0`). *[static]*
- **`0x001225F0`** - Per-rider shadow update entry: calls the shadow build `0x00122898` when rider+0xB18 is visible. *[static]*
- **`0x00122898`** - Rider shadow build, run per sim tick: fits the shadow and renders the silhouette. *[static]*
- **`0x00122D78`** - Estimated finish ticks for a racer still on course (race results screen): raceTicks + remaining / max((origin - remaining)/raceTicks, 30 - place), speeds in cm/tick, with remaining = rider+0x4D0 and origin = +0x4D8 (one reading has 30 - slot instead of place). *[PS2]*
- **`0x00122E50`** - Final-round points estimate for a freestyle/slope-style opponent still riding: s1 = score + 1; result = s1 + cvt.w.s(max(rem - 1000, 0) * (s1 / max(orig - rem, 1))), with rem = rider+0x4D0 (cm) and orig = the route origin distance (+0x4D8, `0x00113130`). *[static]*
- **`0x00122EE8`** - Rider stat read used by stage-script builtins 50/79; the key is mapped through table `0x004897B0`. *[static]*
- **`0x00123128`** - Cited for the CHARDB character order (moby, kaori, allegra, mac, zoe, griff, elise, nate, psymon, viggo); exact role not described. *[unconfirmed]*
- **`0x001231A8`** - Grounded predicate: true in motion 0, or in motion 2 with owner+0x30 == 0; when true, `0x00105398` and `0x00106F78` remove the rider+0x370 ground-normal component from contact normals. *[static]*
- **`0x00123210`** - Rider teleport to a matrix M (rider vt+0x54, stage builtin 34): after gate observer `0x00270970`, P = row3 + 100*(a*row0 + b*row1) with (a,b) by roster slot rider+0x86C: 0:(2,0) 1:(1,0) 2:(2,-2) 3:(2,2) 4:(1,-2) 5:(2,1), else 0; D = row0. Calls score reset `0x00119368(score, 1)` (return discarded), motion exit `0x0011FE78(rider, 0)` (ground entry `0x0013C7A8`), control exit `0x0011FEC8(rider, 0)`, stops the reset fade `0x00125038`, camera set-target `0x0015CC70`, keeps speed = |v|, places via `0x0011D660(P, D, 5, 0.0)`, sets v = forward(+0x1B0) x speed (z kept), FX reset `0x00111890`, then `0x002F1A00`. VU0 chop arithmetic. *[static]*
- **`0x001234D0`** - Places the rider at an instance matrix (rider vt+0x64, stage builtin 64) with no slot offset: `0x0011D660` semantic 5, zero speed, motion 3 / control 0xD, then start-grid control 6. Distinct from builtin 34's teleport `0x00123210`. *[static]*
- **`0x001235F8`** - Stage-script builtins that trigger rider speech (e.g. Hey); probably also the decline push after refusing a Big Challenge offer (unconfirmed). *[static]*
- **`0x00123E30`** - Speech-id dispatch for flag-0x10 contacts (ids from the list built by `0x0027C9B0`): id < 100 -> `0x002A1B58`, ids 100/101 -> `0x002A1B88`. *[static]*
- **`0x00123F38`** - Transport arrival step that calls `0x0011D390` to place the rider. *[static]*
- **`0x001241C0`** - Rider-side NIS (cutscene) actor start, paired with `0x0027F9F8`. *[static]*
- **`0x001242B0`** - Rider ground/scenery probe variant: re-probes the ground for playback riders (rider+0xAC4) and writes the rider-scope ground query result at rider+0x2D4. In cruise mode it reports scenery impacts to `0x00111AA0` (one of that routine's five callers), passing the hit surface velocity as strength. *[static]*
  - `0x001245BC`: Cruise scenery-hit call to `0x00111AA0`: normal at sp+0x60, strength |sp+0x70|, surface at sp+0x9C. *[static]*
- **`0x00124788`** - Rider NIS actor clip playback. Clip time = (start offset + (t - t0)*speed)/60 s at 30 fps, looping at (frames-1)/30; the NIS head, eyes and hands (parts 5, 6, 8, 9) replace the race head/hands. *[PS2]*
- **`0x00125038`** - Stops the rider's viewport screen effect (the reset white fade) via `0x002E4578` when S+0x84 is set, the device index is >= 0 and viewport+0x78 == 1, where viewport = *(S+0x5C+4*dev). No-op for computer riders (device -1). *[static]*
- **`0x001250A8`** - Rider-side one-way volume function; no further details recorded. *[static]*
- **`0x00125108`** - Rider finish routine: calls score finish `0x001193E0` and finish audio `0x00286EA0`. Called at the finish, on the time limit / give up (from `0x00125228` and its give-up branch `0x00125368`, which set the +0x480 DNF flag), and as the handler for runtime event 1 in eligible race modes when the rider has not finished. *[PS2]*
  - `0x001251B8`: Conquer the Mountain run-finish block; chains to `0x00238358` -> `0x00154AB8`. *[static]*
- **`0x00125228`** - Per-rider time-limit check, called from `0x00121818`; implements the freestyle event limit (DNF at 2:00). Requires rider +0x874 (human) and never DNFs a free-ride rider. Once race ticks pass int(limit/60)*60 (via `__fixsfsi` `0x004139F8`; tick 7201 for 2:00) it sets +0x480 = 1 (DNF) and calls the finish routine `0x00125108`; the results store a heat score of 0. *[PS2]*
  - `0x00125368`: Give-up branch, reached while the limit has not expired (timed or not): with rider +0x480 == 1 and the game screen back (*(G+0x84)+0x214 == 4) it calls finish `0x00125108` without the time-up sound. *[PS2]*
- **`0x001253D0`** `cRider_quitEvent` - Give Up for one rider: if unfinished (+0x470 < 0) sets +0x480 = 1 and `0x005366D0`[slot] = 1. *[PS2]*
- **`0x00125448`** - Contains the NIS actor sound start below. *[PS2]*
  - `0x0012584C`: NIS actor sound start: CHARACTER bus, speaker 0 gain, positioned on the actor. *[PS2]*
- **`0x00125970`** - Human-only wind push: adds a steered velocity component once speed exceeds 277 cm/s, using an indexed stat read (a heap address). First routine called by the cruise update `0x0013D818`. *[static]*
- **`0x00125B18`** - Pad vibration motor model: decays the impact level v0 (owner +0xDFC) and slide level v1 (owner +0xE00) each tick in single precision, v0 = v0*0.9133333 - 18.518518, v1 = v1*0.9916667. Large motor f = max((v1-0.5)*0.01, (v0-2)/972.2222), output byte f*205+50; the small motor switches on from (v1-70)*0.00625 or (v0-100)/5000. *[PS2]*
- **`0x001276F0`** - Grab selection: returns the index of the first active Trick1..15 input action (0..14) or -1; reads only actions. Stored as int8 in command byte 2 (bits 16..23) for control states 4/5, so -1 gives 0xFF and the neutral word0 is 0x00FF0000. *[static]*
- **`0x00127848`** - Uber selection: returns the index of the first active UberGrind1..4 input action (actions 27..30) as 0..3, or -1. Used as the int8 Uber identity in control states 7 and 12. *[static]*
- **`0x001278E8`** - Contains the pad vibration gate below. *[PS2]*
  - `0x00127900`: Vibration gate block: the motors run only while racing (game state < 10) and the vibration option (`0x001474E8`) is On. *[PS2]*
- **`0x00127998`** - Human rider input provider (provider interface +0x0C; computer riders use `0x0010A768`): zeroes the 8-byte command (two words), reads the control state via `0x0011FEE8` and switches on the 14-entry table `0x00457EA0` to pack the INPUT.MAP-mapped pad actions, then calls the RLE command recorder `0x0026D178` (motion owner +0xDF8). Axes are trunc(value*31) & 63, decoded as signed 6-bit * float `0x3D042108` (~1/31); flags come from the bool getter. Select = ResetPath, Start pauses, Square = WipeoutRecover. Control 7 packs the identity at bits 17..24, control 12 at bits 15..22 with boost in bits 13/14 (`0x00114130`). *[PS2]*
  - `0x00127DFC`: Input mapping of the GateAnticipate action (26), through `0x00127E04`. *[static]*
  - `0x00127EE8`: Return point (like `0x00127FE0`) after a call in the command-packing path (inferred). *[unconfirmed]*
  - `0x00127FEC`: Control-12 identity packing into command bits 15..22 (`0x00127FEC`..`0x00127FF8`); bits 17..24 is a wrong earlier reading. *[static]*
  - `0x00128280`: Passive-air case: packs CruiseTurn into word0 bits 24..29, CruiseCrouch into word1 bits 0..5, grab identity (`0x001276F0`) into word0 bits 16..23, recovery bit 12, Handplant bit 13, AttackLeft/Right bits 14/15. *[static]*
  - `0x00128500`: Writes command word1 bits 6..11 = action 10 AirAdjRotFB (with `0x00128528`); bits 12..17 are AirAdjRotLR (an earlier decode swapped them). *[static]*
  - `0x00128528`: Second write site of the AirAdjRotFB field (word1 bits 6..11). *[static]*
  - `0x00128630`: Per-tick provider exit; command words, controller/motion state and rider fields are complete here. *[PS2]*

## `src/ai/ai.cpp`

Segment `0x001286A0`-`0x00136C40`.

- **`0x001286A0`** `cAI_cAI` - Called during race game-info construction (call site `0x0022F010`). *[static]*
- **`0x00128958`** - Creates the computer riders a tick after the CTM gate (together with `0x00129E20`). *[PS2]*
- **`0x001289F0`** - Places the riders on the start grid when the list reaches idle (-> `0x001297C8` -> `0x0011D390`, start-grid control 6). *[PS2]*
- **`0x00128AF0`** - Rider manager per-tick update (shared world update). Runs `0x0012BB20` and the race clock `0x00113C20`, builds the participant list and refreshes proximity `0x0010F560`, then runs each per-rider phase over all riders in slot order (human slot 0 first) before starting the next: `0x00120F20`, `0x00121068`, `0x001210B0`, `0x001211F8`, `0x001216E0`, `0x00121700`, `0x00121728`, `0x00121750` (dispatches rider pairs `0x00107888`), `0x001217F8`, `0x00121818`, `0x001218D0`, `0x00121950`. Then the FX passes, section pass `0x00101B60`, `0x001013A8`, and finally increments the game-info +8 total tick count. *[PS2]*
  - `0x00128C00`: Per-rider stage list block: controller update, boost timers `0x001200D0`, triplet approach `0x001211F8`, motion update, pose, motion post/contacts. *[static]*
  - `0x00128EA4`: Calls `0x00121818` for each rider (selected contact -> slot-2 stage programs), before any FX pass. *[PS2]*
  - `0x00128F20`: Start of the FX update passes (to `0x001290F0`), in order: light list, wake, board sparks, board track, boost, power-up aura, air streamers, rival indicator, locator beam, snow FX, fist sparkle, cheat toggle. *[static]*
  - `0x00128F80`: Board-track loop: updates every rider's board track before the snow-spray loop `0x001290A0`; the order matters because both draw from the shared visual LCG `0x004A3AFC`. *[static]*
  - `0x001290A0`: Snow-spray loop over all riders, after the board-track loop; shares the visual LCG word `0x004A3AFC`, so interleaving track/spray per rider would change the jitter sequence. *[static]*
  - `0x00129124`: Section pass call `0x00101B60` after all riders (slot-1 section programs, flag grid builds, set-piece seeds). *[PS2]*
  - `0x00129134`: Increments game-info +8 (total simulation ticks), through `0x00129144`. *[static]*
- **`0x001296F8`** - Loads the computer riders one step per tick (-> `0x0011C298`) while the fly-over plays. *[PS2]*
- **`0x001297C8`** - Start-grid placement step between `0x001289F0` and `0x0011D390`; also described as the run-restart path, which records statistics with no score or time. *[static]*
- **`0x001298C8`** - Global game tick accessor: returns *(*(*(*`0x004A28A8`+0x84)+0x0C)+8) (global at gp-0x848). Used for the camera's last-airborne tick (+0x2CC), jump takeoff ground-focus and lastLeave stamps, upper-reaction cooldowns and hair scheduling; advances by 1 per tick. *[PS2]*
- **`0x00129E20`** `cAI_initComputerRiders` - Computer rider creation step (together with `0x00128958`). *[static]*
- **`0x0012A180`** - Tests that every rider is loaded (clears 'Loading...'). *[PS2]*
- **`0x0012A250`** - Returns true when every configured human rider has a nonnegative finish marker (true for an empty list). Decides the round; when true, `0x0011A228` returns immediately, so a post-finish landing commits no trick and the combo clock stays closed. *[PS2]*
- **`0x0012A340`** - Path-bank delivery on a location read: replaces the race/reset path bank `0x004D33A0` for locations with id < 22, then re-attaches the human (`0x00112180`). Hubs use variant 1 in a time challenge and variant 2 in a points challenge (1 in mode 11); everything else uses variant 0. The AIP record can be dispatched before the row leaves state 6 when the chunk has more to read. *[PS2]*
- **`0x0012A490`** - Drops a location's path bank when the location is evicted; until the next read there is no bank, so race progress and the route stop moving. *[static]*
- **`0x0012B090`** - Run quit path; records statistics with no score or time. *[static]*
- **`0x0012B180`** - Commits, then clears, the free-ride score. *[static]*
- **`0x0012B788`** - Iterates all primary riders and calls `0x00120E50` for each (rider light/scope refresh); has no refresh gate of its own. Called by the world job `0x0026DBF0` at `0x0026DD90` after the stream/world update work. *[PS2]*
- **`0x0012BB20`** - First per-rider pass run by the rider manager update `0x00128AF0`. *[static]*
- **`0x0012BE20`** - Start-grid control (control 6) entry, entered fresh at race start: copies the reference stance, clears the animation graph, resets root translation, picks the start semantic (1 when the incoming phase is 2, else 0) and clears all four phase values. Requests root half-angle -pi/2 for switch stance, else -0. *[PS2]*
- **`0x0012BF68`** - Start-grid control (control 6, race start) update dispatch. Input is a signed 6-bit value in word0 bits 12..17. Phase 0 updates lean and push-off timing (the formula applies while steadyTime < 0.4, not >= 0.4 as once read); phase 1 does crouch, launch velocity and the motion handoff; phases 2/3 play the manual-start animation. Stops boost. Release gates `0x0012C028`/`0x0012C078`. *[static]*
- **`0x0012C028`** - Start-grid control (control 6) release gate used by the update `0x0012BF68`. *[static]*
- **`0x0012C078`** - Start-grid control (control 6) release gate used by the update `0x0012BF68`. *[static]*
- **`0x0012C0C0`** - Start-grid control (control 6) phase function, probably one of the phase handlers of `0x0012BF68`. *[static]*
- **`0x0012C130`** - Start-grid control (control 6) phase function, probably one of the phase handlers of `0x0012BF68`. *[static]*
- **`0x0012C230`** - Start-grid control (control 6) phase function, probably one of the phase handlers of `0x0012BF68`. *[static]*
- **`0x0012C408`** - Start-grid control (control 6) phase function, probably one of the phase handlers of `0x0012BF68`. *[static]*
- **`0x0012C678`** - Finish control 10 update (post-finish coast: brake, stop, finish reaction), requested when rider +0x470 >= 0. Phase 0: turn target +-0.1, boost off, semantic 11, velocity x0.97, brake target 1. Phase 1: velocity x0.93 until |brake| < 0.2 and turn 0, then `0x0010E028`(+0x100 ? 1 : 4) and semantic 4. Phase 2: finish reaction at rate 0.75 (then rate 1), +0x470 = max(+0x470, 10). Phase 3: stopped. *[PS2]*
  - `0x0012C964`: Stop-clip end: raises rider +0x470 to 10. *[PS2]*
- **`0x0012CB68`** - Crash control (control 8) update with the crash recovery meter (calls `0x0029E970` / `0x0029E590`); consumes ragdoll collision events and plays ragdoll contact sounds. The meter tests command bit 0x2000 (human category 1); the input is WipeoutRecover (Square pressed), not held jump as earlier notes said. *[PS2]*
- **`0x0012CD20`** - Crash control phase 0: starts the crash slide loops; also plays ragdoll contact sounds. *[static]*
- **`0x0012D160`** - Ground crash (crash phase 2, slide): consumes ragdoll collision events and exits to phase 3 (get-up) or phase 1 (air), both stopping the slide loops. At `0x0012D218` it requests a camera shake via `0x0015E360` (index 4, fade 0, scale clamp(speed_cmps*0.036/100, 0, 1)), gated by rider +0x870 >= 0 and +0x87C != 0; that callback is camera shake, not rumble as an earlier label said. *[PS2]*
  - `0x0012D218`: Call to camera requestShake `0x0015E360` (index 4, fade 0, speed-scaled). *[static]*
  - `0x0012D23C`: Crash slide rumble (phase 2 only): sets owner+0xE00 = 0.5 * the playback base from `0x0012E528`. *[PS2]*
  - `0x0012D28C`: Ragdoll impact rumble site: owner+0xDFC = max(v0, a). *[PS2]*
- **`0x0012D4E8`** - Crash ragdoll / air-phase crash-loop path (phase 1 slide loops): plays the crash sound through `0x00296310` and consumes ragdoll collision events via observers. *[PS2]*
  - `0x0012D6F8`: Ragdoll impact rumble site: owner+0xDFC = max(v0, a). *[PS2]*
- **`0x0012D848`** - Crash get-up routine; calls the landing sound `0x002948D0`. *[static]*
  - `0x0012D8A0`: Ragdoll impact rumble site: owner+0xDFC = max(v0, a). *[PS2]*
  - `0x0012D8E8`: Crash phase-3 get-up slide event: slide rumble owner+0xE00 = 2 * (1 - clip progress). *[PS2]*
- **`0x0012E010`** - Crash reset-clip root transform: composes the scaled local root with the inverse preview root. *[static]*
- **`0x0012E528`** - Returns the playback base used for the crash-slide rumble amount (0.5 * it). *[static]*
- **`0x0012E690`** - Control 8 (crash) exit (table `0x00456B90`): after a +0x6C0 voice query, requests reaction kind 4 (wipeout speech) on even logic ticks. *[PS2]*
  - `0x0012E6BC`: Get-up block: requests reaction kind 4 (human only). *[static]*
- **`0x0012E778`** - Control 3 (soft collision reaction) update: calls `0x00116378`, `0x00116120`, then rail attach `0x00106848`; retargets scaled turn, crouch and brake but writes no velocity. If attach succeeds it returns and the rider stays in control 3 on the rail (no 13/7 requests) until the soft clip ends (primary sequence +0xC0 completion), then requests control 7. *[PS2]*
  - `0x0012E850`: On a rail, passes RailBalance (word0 bits 21..26, not CruiseTurn) to `0x00113F38`. *[PS2]*
- **`0x0012E980`** - Control 2 enter (gain-focus handler): sets the rider +0x1FC target, with +0x204 = 0 and +0x200 = 1/30, before that tick's `0x001211F8`; leaves the charge triplet unchanged. *[PS2]*
- **`0x0012E9B0`** - Control 2 exit (table `0x00456B90`). *[static]*
- **`0x0012E9B8`** - Control 2 update (prewind / charged jump while jump is held); also calls rail attach `0x00106848` and the take-off sound `0x00294170`. Tests JumpHeld (bit 0x2000) at `0x0012EA24`; while held, requests full charge via `0x00113F88`(rider,1,0). Spin/flip inputs threshold at +-0.2 to prewind targets -1/0/+1 at rate 5.000027*|target-current|/60 for style 0, else 5.00009*|target-current|/60. Branches for animation class 10 and semantic 21 (zero request at rate 0.08333379); when +0x328 == 0 and +0x2DC == 0 runs the reverse turn `0x00114CC0`. The release path `0x0012EA30` ends in control 5. *[PS2]*
  - `0x0012EA24`: Tests decoded command bit 0x2000 (JumpHeld) to choose the held or release path. *[static]*
  - `0x0012EA30`: Jump release path: snaps the filtered prewind (angular step `0x3F490FDC`, deadzone 0.2), takes off, zeroes targets, enters control 5 (`0x0011FEC8` -> `0x00111538` -> `0x00111630` -> `0x00133128`); control 5 first runs next tick. *[static]*
  - `0x0012EA4C`: Air-release root reset (to `0x0012EAC0`) when rail style rider+0x328 is 3 or 4: anim+0x30 = `0x004FF130`, anim+0x40 = sincos(-0) about `0x004FF160`; runs before `0x0012EE30` plays the release clip. *[PS2]*
  - `0x0012EAD8`: Clears rider+0x328 (rail style) after the air release. *[PS2]*
  - `0x0012EAE4`: Release while already airborne (to `0x0012EB1C`): skips takeoff and motion re-entry and goes to control 5, with no new impulse or trajectory reset. *[static]*
  - `0x0012EAFC`: On release when not airborne, calls takeoff `0x00114298`(rider, rider+0x220 charge). *[PS2]*
  - `0x0012EB08`: Calls `0x0011FE78`(rider, 1) to set the air motion mode after takeoff. *[static]*
  - `0x0012EB70`: While jump is held, calls `0x00113F88`(rider, 1, 0): full charge, no brake. *[static]*
  - `0x0012EC1C`: On a rail (style != 0), passes clamp(word1 bits 6..11, +-0.5) as RailBalance to `0x00113F38`; spin comes from PrewindSpin (D-pad). *[PS2]*
- **`0x0012EE30`** - Directional clip selection for prewind and the charged release: angular classifier over the five-style tables `0x0043D788`/`0x0043D840` (prewind definitions 245..267); plays the control-2 release clip. *[static]*
- **`0x0012F230`** - Reset control 9 (forced reset) entry: sets progress 0 and freezes the main channel, and if allowed builds the white in/out reset fade (`0x002E47E8`, durations .5, 0, .5) bound to a pointer to reset progress; no captured-frame texture. *[static]*
- **`0x0012F398`** - Reset control 9 update, first stepping on the tick after the request: progress rises to 1 at 0.025*timeScale with the main animation frozen. At 0.5 it re-routes and places the rider (`0x00112D58`, `0x0026A8B8`, `0x0011D660` with 200 cm clearance or 1000 cm in game mode 2, semantic 287, `0x0011DF18`) and passes the meter penalty from `0x00119368` to `0x0010E098`; at 1 it sets playback rate 1, control 4 and motion 1. At timeScale 1, placement is on tick 21 and the handoff on tick 41. `0x0012F588` gates device completion. *[PS2]*
  - `0x0012F468`: Reset clearance selection block: 1000 cm for game mode 2, else 200 cm. *[static]*
- **`0x0012F588`** - Reset gate for fade creation and device-specific completion work: true only for an active reset with an enabled, nonnegative human device. *[static]*
- **`0x0012F620`** - Control 4 (passive air) entry, distinct from explicit-jump control 5: captures angle = atan2(currentCrouch, currentTurn) and magnitude = max(|currentTurn|, currentCrouch), sets both latches to 1 and last identity -1, and sets prewind rates rider +0x2A4/+0x2B0 to 1/30 with targets 0 (these rates survive crashes from controls 3/4). Current values and turn/crouch/brake are kept; no animation request. State lives at motion owner+0x210. *[PS2]*
- **`0x0012F730`** - Control 4 (passive air) update, entered the tick after passive departure. Calls recovery `0x00116120`, upper action `0x001163B0`, optional handplant `0x00107578`, rail attach `0x00106848`, then `0x00114130`(false,false), so boost stops here. A held attack (its +0x8 latch) returns before handplant, rail attach and targets, keeping control 4; a failed handplant forces crouch request 1; the upper return is checked only while the upper latch is set. Release sets magnitude -1, targets 0, rates `0x3D4CCCCE`; picks 9/10 unless class 9 is settling, and with turn, brake and crouch 0 and the latch released requests 287 then control 5. Leaves rider +0x2DC unchanged. *[PS2]*
- **`0x0012FB68`** - Control 4 (passive air) exit (table `0x00456B90`), run when the next control is not 0: an upper class of 3 or 13 gets a 0.1 s fade on channel 0, and the channel-1 sequence rate is set to 1 (the channel mismatch is original behaviour). *[static]*
- **`0x0012FC60`** - Board-press control (control 1) enter. *[PS2]*
- **`0x0012FC80`** - Board-press control (control 1) update. `0x00116378`, `0x00116120`, rail attach `0x00106848`, `0x0012FFF8` and `0x001162C8` may each end the tick; then `0x00114130`(BoostHeld, BoostPressed) and `0x00113F88`(0,0); on a rail (+0x328 != 0) `0x00113F38`(clamp(RailBalance, +-0.5)), on the ground `0x00113E80`(clamp(CruiseTurn, +-||+0x280| - 0.5|)); then `0x00115B58`, `0x00115D48` and the phase handler (with `0x001161D0`). *[PS2]*
- **`0x0012FE98`** - Board-press control (control 1) exit (table `0x00456B90`). *[PS2]*
- **`0x0012FEC8`** - Board-press stance flip, run as a board pivot starts. *[PS2]*
- **`0x0012FFF8`** - Board press while airborne in control 1: plays 27/35 and requests control 5, or first finishes an active pivot (phase 2, scores `0x00119AD8` then `0x00119A38`, plays 287). Runs with motion 1; after a lost rail it runs on the next tick. *[PS2]*
- **`0x00130228`** - Board-press phase 0 (press in): +0x274 -> 0.5 at ts/60 and +0x268 -> 1 at 2*ts/60. When +0x268 reaches 1 it scores `0x001199F8` and enters phase 1 with 28 (nose, +0x280 = 0) or 36 (tail, +0x280 = 1); releasing first goes to phase 3 (25/33). *[PS2]*
- **`0x001303E0`** - Board-press phase 1 (hold): runs reverse `0x001306B0`, R3 ollie `0x001307B8`, BoardPivot `0x001308D8`, depth `0x00130DD0` and release check `0x00131200` in that order. *[PS2]*
- **`0x001304D0`** - Board-press phase 2 handler: sets phase 1. *[static]*
- **`0x001304E0`** - Board-press phase 3 (release): waits for +0x268 to empty and scores `0x00119A38`, then exits to cruise via `0x00115640` and control 0, or on a rail plays `0x001326C8` and returns to control 7. A re-press returns to phase 0 with 24/32. *[PS2]*
- **`0x001306B0`** - Board-press reverse: a reverse turn (`0x00114CC0`) swaps nose and tail via semantics 23/31; completion kind 9 continues into 28/36. *[static]*
- **`0x001307B8`** - Board-press R3 ollie: latches R3; on release finalises the pivot, plays 35 when |+0x280| > 0.5 else 27, clears the press style, calls takeoff `0x00114298`(rider, 1.0), sets motion 1 and control 5; plays the take-off sound `0x00294170`. *[PS2]*
- **`0x001308D8`** - BoardPivot: rotates the board pivot +0x280 toward the right-stick angle. Springs back (`0x001313A8`) if both sticks are < 0.1, travelling backwards or R3 is latched. Otherwise compares the stick atan angle (quadrant fixed, wrapped to +-pi) with +0x280*pi: if not pivoting and the difference is >= 27 deg it kicks +0x280 to +-0.85 or +-0.15, else rotates at delta/60*2ts. Inside 0.15..0.85 plays 29/30 (nose) or 37/38 (tail), flipping stance (`0x0012FEC8`) as a pivot starts. *[PS2]*
- **`0x00130DD0`** - Board-press depth update: the +0x274 target is the press, limited to 0.5+t/2 for the first second, then snapped to 0 or 1; the rate blends curves `0x004FE920`/`0x004FE940` by min(t*0.2, 1) (rate 1 when crossing 0.5). Holding full depth for over 1 s triggers a hard crash (not an effect, as earlier notes said): `0x0010EB30`(rider, 358/359, 0, rider+0x438, block) with position, velocity direction, -Z and speed. Also triggers the Whooh speech. *[PS2]*
- **`0x00131200`** - Board-press release check: 0.5 s idle or the opposite stick past 0.8 sends +0x274 to 0; the press ends (phase 3, 25/33) once +0x274 < 0.5. *[static]*
- **`0x00131348`** - Board-press jump-out: leaves control 1 for a charged jump (control 2, semantic 245). Also run by the soft-collision handler `0x00108388` before it enters control 3. *[PS2]*
- **`0x001313A8`** - Board-press pivot spring-back. *[PS2]*
- **`0x00131428`** - Board-press pivot finalise. *[PS2]*
- **`0x00131608`** - Control 0 enter (idle reset): zeroes the idle clock rider+0x35C and clears the jump/board-press latch +0x360. *[PS2]*
- **`0x00131620`** - Control 0 (cruise / normal ground) update. First runs preconditions `0x00116378` and `0x00131CC0` (returns at once if the latter diverts to passive air), then tries handplant `0x00107578`, rail attach `0x00106848`, jump request `0x001162C8`, board-press entry `0x001161D0` and attack `0x001163B0` in that order. Then boost dispatch `0x00114130`, `0x00115B58`, `0x00115D48` and the main animation selection block `0x00131878`; picks slow-crouch semantic 22 below 833.3 cm/s from the crouch axis. Its game-RNG draws (e.g. prewind entry) belong to the stage-1 controller pass. *[PS2]*
  - `0x00131794`: Call to `0x001162C8` (jump press -> control 2). *[static]*
  - `0x0013179C`: Exit taken when `0x001162C8` returns nonzero, skipping the charge request. *[static]*
  - `0x001317D8`: Call to board-press entry `0x001161D0`, after the jump request and before the attack. *[PS2]*
  - `0x001317E8`: Call to attack control `0x001163B0`, reached only after the handplant, rail, jump and board-press attempts. *[PS2]*
  - `0x00131804`: Turn-target clamp to [-0.5, 0.5] while an attack is held (to `0x0013182C`). *[static]*
  - `0x00131844`: Call to boost dispatch `0x00114130` (can set rider+0x2FC = 0.25) before animation selection. *[PS2]*
  - `0x00131868`: Call to the reaction-request player `0x00115B58` (before `0x00115D48`). *[PS2]*
  - `0x00131870`: Call to the upper-body reaction routine `0x00115D48`, after the turn/crouch targets and `0x00115B58`. *[static]*
  - `0x00131878`: Main ground animation selection block (to `0x00131C04`), run before `0x001211F8`: 22 kept; 21 or reversal zero turn/brake targets; class 10 keeps landing; class 5 keeps semantic; else cycle. Tuck + boost (rider+0x2FC) picks 8. *[PS2]*
  - `0x001318EC`: Call to the reverse-stance check `0x00114CC0`, after the target requests and before the landing-class hold. *[static]*
  - `0x00131A4C`: Reads the rider+0x2FC boost scalar for semantic selection. *[static]*
  - `0x00131B24`: Reads the rider+0x2FC boost scalar for semantic selection. *[static]*
- **`0x00131C30`** - Control 0 (normal ground) exit (table `0x00456B90`). Probably the exit that, when the next control is not 4, fades channel 0 and restores the channel-1 rate while the upper class is 3/13 (inferred). *[static]*
- **`0x00131CC0`** - Airborne divert check, run by cruise before the handplant test: if motion is already 1 (airborne) it requests control 4 (passive air) and returns 1, so control 0 becomes control 4 on the following frame. *[static]*
- **`0x00131D08`** - Rail control (control 7) enter: control+0 = 0 (spin accumulator), control+4 = -1 (identity byte), rider+0x360 = 0 (board-press latch), animation-turn triplet +0x1FC rate 1/15 target 0. Not run when control 3 (soft) attaches to a rail. *[static]*
- **`0x00131D30`** - Rail control (control 7) update. Order: recovery `0x00116120`, motion-1 wait `0x00132770`, crouch/jump `0x001162C8`(bit 14 held, bit 13 pressed), Uber gate `0x00132620`, boost `0x00114130`, balance steer `0x00113F38` (RailBalance unnegated), `0x00113F88`(0,0), `0x00115B58`, `0x00115D48`. Then spin `0x00132060` or board-press entry `0x001161D0`; else balance target clamp(motion+0x18*1.2, +-1) at rate 1/15, re-playing 18/19/20 by style when `0x00312AA0` differs. A class-10 entry branch returns early. *[PS2]*
  - `0x00131E00`: Passes RailBalance (command word0 bits 25..30 * 1/31) unnegated to `0x00113F38`; stick left gives negative RailBalance (LStickR - LStickL), so left goes left. *[PS2]*
  - `0x00131E80`: Call site of board-press entry `0x001161D0` (also `0x00131ED8`). *[PS2]*
  - `0x00131F7C`: Class-10 branch (to `0x00131F98`): sets the balance target and returns without requesting a cycle, so the 68/69/70 air-entry clip keeps fading in instead of 18/19/20 playing over it. *[static]*
- **`0x00132048`** - Rail control (control 7) exit (table `0x00456B90`): balance triplet +0x238 target 0 at rate 1/15. Runs when the rider jumps on a rail (control 2 requested via `0x001162C8` -> `0x0011FEC8`) and on a select warp from a rail (with `0x0013C5A0`). *[PS2]*
- **`0x00132060`** - Rail spin (left flag): changes grind style by table, plays spin semantics 49..54, updates switch/balance/root and scores via `0x00119918`. Style 1: L->3 sem 49 root +pi/4, R->4 sem 50 root -pi/4. Style 2: L->4 sem 53, R->3 sem 52 (switch 0, balance negated). Style 3: L->2 sem 51 (switch 1, negated, root -pi/2), R->1 sem 51. Style 4: L->1 sem 54, R->2 sem 54 (switch 1, negated). control+0 -/+= pi/2. *[static]*
- **`0x00132620`** - Rail Uber entry gate called by rail control 7 after the recovery/upper-action gates; exclusive shoulder masks 1/2/4/8 give identities 0..3. Identity -1 resets the remembered ordinary-rail identity and returns false. If rider+0x2F0 (Tricky time) > 0 and rider+0xB2C bit 1 is set, stores the identity at owner+0x394, requests control 12 and returns 1 (ordinary latch untouched); otherwise a changed identity only updates trick naming (`0x0028B180`) and calls the denied sound `0x00299B70` once, then updates the latch. *[static]*
- **`0x001326C8`** - Plays the rail cycle or rail landing semantic for the grind style: grounded (a1 = 0) style 1/2 -> 18, 3 -> 19, 4 -> 20; airborne (a1 != 0, controls 4/5) 1/2 -> 68, 3 -> 70, 4 -> 69; blend -1. Callers include rail attach `0x00106848`, control 3 `0x0012E778`, board-press release `0x001304E0` and the control-12 finish; it restores the rail animation after a light impact and when returning to control 7. Probably passes 0 as the 4th argument of `0x003128E8` (inferred). *[static]*
- **`0x00132770`** - Rail control helper for motion 1: waits for bit 0 of a class-14 clip, restores stance (`0x00115640`), then requests control 4 (rotate = 0) or 5. *[static]*
- **`0x001328B0`** - Handplant control 11 enter; calls the handplant sound `0x0029DC48`. *[static]*
- **`0x001329B0`** - Handplant control 11 setup. *[static]*
- **`0x00132A30`** - Control 11 (half-pipe / handplant) update: calls rail attach `0x00106848` and writes the half-pipe balance triplet +0x244. *[PS2]*
- **`0x00132F98`** - Control 11 (half-pipe / handplant) exit (table `0x00456B90`): zeroes the half-pipe balance triplet +0x244. *[static]*
- **`0x00132FB8`** - Handplant clip play with reflect (uses table `0x00459FC8`). *[static]*
- **`0x00133128`** - Air control (control 5) entry: seeds the angular state from the filtered prewind rider+0x2A4/+0x2B0 (initial targets 180 deg spin, 360 deg flip; prewind gives different initial rates and a continuation mode) and consumes the spin stat. Writes the charged-release playback rate through sequence+0x90. *[static]*
- **`0x00133308`** - Control 5 (active air / spin) update: computes the air targets, writes board-press style rider+0x330 each tick, and consumes the spin stat. Calls rail attach `0x00106848` and the in-flight stance switch `0x00135BE0` (in phase 3 before D-pad input, and in unfinished phase 2 when no grab is active and the channel-2 request is not 288). Handplant entry is tried only when air-control +0x0C == 3. Passes false,false to boost `0x00114130`, since command bit 14 means tweak in this state. *[PS2]*
  - `0x001333E0`: Air control step block (to `0x00134334`): late-spin modes (LateSpin = word0 bit 15; mode 0 + LateSpin + adjust -> 2, spin/flip -> 0, mode 2 without LateSpin -> 1; modes 1/2 take spin/flip from the adjust axes), then spin/flip phase, target and rate updates. *[PS2]*
  - `0x00133590`: Block (to `0x00133634`) that writes board-press style +0x330 every air tick, from the BoardPress step and pre-step adjust flip. *[PS2]*
  - `0x0013364C`: Captures the pre-update phase (register s3); the animation selector needs this phaseBefore plus the effective spin/flip after mode substitution, snapping and phase handling. *[static]*
  - `0x00133734`: Phase-3 block: starts a new D-pad trick (calls `0x00119C98`). *[PS2]*
  - `0x0013437C`: Control-5 animation selector block (to `0x00134C6C`), after angular/scoring updates: grab or 288 keeps clip; adjust -> 297..304; phase 3->0/1 -> 269..276; phase 1 -> 293..296; new phase 2 -> 306..313 via `0x004581A0`; else 287/305. No RNG draws. *[static]*
  - `0x00134B80`: Prediction-driven landing-animation branch (to `0x00134C3C`). *[static]*
- **`0x00134CB0`** - Control 5 (air) exit (table `0x00456B90`), also run by requestControl(13) during rail attach and before a spin-to-handplant entry. Re-invokes the air presentation `0x00134DD0` as an exit event and bakes the presented pose, rebuilds orientation (`0x0011E098`), quantizes spin (+0x2DC, sign restored from progressSpin/targetSpin), clears the flip rate, restores adjustment targets/rates, zeroes the prewind triplets and fades animation channel 1 over 0.33 s. Not invoked on touchdown from held control 2 or passive control 4. *[PS2]*
- **`0x00134DD0`** - Air presentation update, advanced once per airborne logic frame (also called as an exit event by `0x00134CB0`): shifts to the skeleton pivot (the animated local bone selected by rider+0x89C times body scale), appends a local-Z rotation by -(totalSpin+adjustSpin), updates the flip-axis blend from the two angular rates, rotates by totalFlip+adjustFlip about the blended axis, shifts back, then calls `0x0011FA10`. The air control step does not advance the axis blend. Not used for crash roots. *[PS2]*
- **`0x00135180`** - Air presentation: physical-to-presentation pose (together with `0x00134DD0`). *[static]*
- **`0x001352A8`** - Grab/tweak/Uber lifecycle state machine (states 0..5); returns a grab-active flag (main classes 18..20, using the class cached before a state-5 reset). Own state is only phase owner+0x234 and grab index owner+0x238. Ordinary grab (states 0/1/2/5): begin at marker 0, hold at marker 1 with rate 0, resume on release, reset at marker 3; the index resolves via `0x00150118`. During Tricky it looks up the Uber for the grab slot (-> `0x00150198`/`0x001502C8`/`0x001503F8` -> `0x0014FEA8`, table `0x0045AEB8`), sampling the tier variant once per call from boostTier >= 5. Emits play/fade, score `0x00119708`/`0x001197D8` and grab sound `0x0029A530` requests. *[PS2]*
  - `0x00135480`: Rate write (with `0x00135488`): gets the first sequence via `0x00311B20` and writes only its +0x90 rate. *[static]*
  - `0x00135488`: Write of the first animation sequence's +0x90 rate. *[static]*
  - `0x00135554`: Rate write (with `0x0013555C`) to the first sequence's +0x90. *[static]*
  - `0x0013555C`: Write of the first animation sequence's +0x90 rate. *[static]*
- **`0x00135BE0`** - In-flight stance switch in air control when the left stick turns the rider past ~90 deg (test `0x00114DB8`). On a switch: +0x110 += rotate(q, 2*(scaled pivot xy, 0)), `0x0011E098`, stance flip `0x00115168`, new flight `0x001135B8`; wraps total spin by pi into [-pi, pi), negates four flip fields and plays 288 (clip 0x7500, 0.3 s, completion 1 -> 287). *[PS2]*
- **`0x00135CB0`** - NPC air rotation time estimate (together with `0x00135DB0`). *[static]*
- **`0x00135DB0`** - NPC air rotation time estimate (together with `0x00135CB0`). *[static]*
- **`0x00136268`** - Control 12 (rail Uber) enter: sets the root by grind style and plays the INTO clip from the Uber record (style 4: root +pi/4, rec+0x00; 3: rec+0x04, no root; 2: +0x320 = 0, anim+0x18 = 0, root, rec+0x08; 1: root, rec+0x0C). Then `0x00119938` -> `0x0010E098`, rider+0x328 = 3, phase 0, `0x00116930`, and head/upper fades `0x00311E88`(anim,1,0.33) and (anim,0,0.33). *[static]*
- **`0x00136508`** - Control 12 (rail Uber) update. After `0x00116120`, a re-attach via `0x00106848` plays LAND (phase 1) and returns; then `0x00114130`, `0x00113F38` (RailBalance) and `0x00113F88`(0,0). Phase 0 waits for `0x00312AE8`(anim,2), then CYC; phase 1: identity change or motion 0 -> OUTOF, else CYC or BAL_L by |+0x238| < 0.1; phase 2 finishes to `0x001326C8` + control 7, or `0x00115640` + control 5/0. Commits the score via `0x00119958`, then counts +0x2F4 and at 10 sets +0x2F0 = 60 (Uber tier increment). *[static]*
  - `0x001365DC`: Passes RailBalance (word0 bits 23..28 * 1/31) unnegated to `0x00113F38`. *[PS2]*
  - `0x00136888`: Forwards the award from Uber score end `0x00119958` to boost award `0x0010E098` (to `0x00136890`), before the Uber counter update. *[static]*
- **`0x00136958`** - Motion mode 3 (frozen/reset) update, active at the race-ready gate: calls `0x0011E098` to rebuild orientation. Paired with `0x00136978`. *[PS2]*
- **`0x00136978`** - Motion mode 3 (frozen/reset) no-op stage, paired with `0x00136958`. *[static]*

## `src/ai/motion/wipeoutmotion.cpp`

Segment `0x00136C40`-`0x00138B48`.

- **`0x00136C40`** `cWipeoutMotion_gainFocus` - Wipeout (crash, motion 2) entry: uses the airborne crash submode for any previous motion other than 0 and restarts the rider trajectory predictor. A crash from the air runs it before that tick's camera step, so the camera landing angle is 0. *[PS2]*
- **`0x00136D40`** - Crash board detachment; handles crash class 22 (Uber crash that detaches the board). *[PS2]*
- **`0x00136E98`** - Wipeout motion (mode 2, crash/ragdoll) update: calls `0x00136F30`, then the sliding routine `0x00137D18` when state +0 == 0, else the airborne routine `0x00137750`. Paired with `0x00136EE0`. *[PS2]*
- **`0x00136EE0`** - Wipeout motion (mode 2) companion stage, paired with `0x00136E98`: selects `0x00138640` or `0x00137860`. *[static]*
- **`0x00136F28`** - Crash motion exit. *[static]*
- **`0x00136F30`** - Detached board root integration (position/quaternion, gravity -1800, XY drag -0.2); called first by the wipeout update `0x00136E98`. *[static]*
- **`0x00137750`** - Crash airborne predictor/alignment; selected by `0x00136E98` when wipeout state +0 is nonzero. *[static]*
- **`0x00137860`** - Crash-motion (motion 2) collision step for the airborne body, selected by `0x00136EE0`: body query with cache rider+0x868, terrain landing probe `0x00138960`, then the instance-contact phase `0x00105398`; writes +0x370/+0x3D0. On a contact it pushes, drops closing velocity and restarts the predictor, with no `0x00105D98` reaction (collision history not updated). Reports a snow impact via `0x00111AA0` at `0x001379D4` only if the probe fraction >= the float at `0x0049BE88` (gp-0x7268), normal speed <= 0 and the body is within 10 cm of the hit. Shares rider caches with ground contact; entering control 8 does not clear them. *[PS2]*
  - `0x001379D4`: Air body landing call to `0x00111AA0`: probe point sp+0x3A0, normal sp+0x3B0, strength dot(vel, normal), surface sp+0x3EC. *[static]*
- **`0x00137D18`** - Crash (motion 2) sliding forces and terrain contact (not ordinary grounded riding): surface +0x90 slip curve, crash damping, fixed 1800 cm/s^2 gravity and special drag, angular speed pulled toward linear speed*0.0125, speed cap; position integrates before acceleration and the old material +0x18 depth is kept. Writes +0x370/+0x3D0 and on penetration reports a snow impact via `0x00111AA0` at `0x00138454` (deep branch strength |surfaceVelocity|, shallow branch |velocity| before correction), storing point/normal/surface on the rider. *[PS2]*
  - `0x00138454`: Sliding contact call to `0x00111AA0`: hit point s2, normal sp+0xC0, surface sp+0xFC. *[static]*
- **`0x00138640`** - Wipeout stage selected by `0x00136EE0`: crash rider-scope world query that runs the instance-contact phase `0x00105398`, followed by the crash post / rider pair point. *[static]*
  - `0x001388D4`: Crash contact finish stage block. *[static]*
- **`0x00138960`** - Crash contact/landing terrain probe used by `0x00137860`: probes +-200 cm along the normal through `0x003342D0` with cache rider+0x864, preferred fraction 0.575; returns point/normal/surface and also writes rider+0x2D4. *[PS2]*

## `src/ai/control/handplantcontrol.cpp`

Segment `0x00138B48`-`0x001448D8`.

- **`0x00138B48`** `cHandplantMotion_gainFocus` - Handplant motion (motion 5) enter. *[static]*
- **`0x00138BA0`** - Handplant motion-5 setup: the direction to the lip is measured from the head bone rider+0x8A8, not the board root. Its relative.w <= 0 branch is unreachable with finite inputs. *[static]*
- **`0x00139178`** - Handplant motion (motion 5) exit. *[static]*
- **`0x001391A8`** - Handplant motion 5 update: computes the launch once (sets +0x10 nonzero), seeds the predictor at the lip +5 cm Z and steps it 8 times, then until status 1 or 3; later pinned ticks align the rider with `0x00121AA0` (gain 2*ts, max rate 6.632*ts). *[PS2]*
- **`0x00139528`** - Handplant motion 5 post stage; calls `0x0011E150`. *[static]*
- **`0x00139548`** - Handplant launch velocity by phase: sets the lateral push, then overwrites the vertical. Phase 3: 0.7*entrySpeed on the tangent minus lateral 2 (rail 15)*27.78 by lip side, vertical 0 (rail 10*27.78). Phases 4/5: lateral 5 (rail 15)*27.78 toward/away from the lip, vertical 0 (rail 5*27.78). Phase 6 (not phase 1, as once read): 833.33 on the tangent, +416.67 vertical. *[static]*
- **`0x001399E0`** - Air motion (motion 1) enter / gain focus: seeds the flight `0x001135B8`(rider+0x788, rider+0x110, rider+0x1E0) from the post-takeoff position and velocity on the departure tick (also for crashbag bounces), sets air +0 when the previous motion was 5, and sets the ground-leave presentation targets, which must precede the `0x001211F8` filter tick on a charged release. *[PS2]*
- **`0x00139A18`** - Motion 1 (airborne) exit: empty (bare return). *[static]*
- **`0x00139A20`** - Airborne motion (motion 1) update, receiver motion owner +0x20: calls the air trajectory updater `0x00113648` first, copies the retained presentation up rider+0x180 into contact normal +0x370 and clears surface velocity +0x3D0, runs the orientation tail (`0x00139A64`..`0x00139C68`), then the touchdown probe and response `0x00139C88`. *[PS2]*
  - `0x00139A64`: Air alignment block (to `0x00139C68`): aligns the rider toward the predicted landing normal/heading via `0x00121AA0`, gated by prediction status/surface/flags with a remaining-time gain; heading correction is suppressed during manual adjustment. *[static]*
  - `0x00139A70`: Block (to `0x00139A8C`) copying the retained presentation up rider+0x180 (previous board-root up) into contact normal +0x370 and clearing surface velocity +0x3D0. *[PS2]*
  - `0x00139A80`: Part of that copy: zeroes +0x3D0 while keeping the ground tangents +0x3A0/+0x3B0. *[PS2]*
- **`0x00139C88`** - Motion 1 (airborne) post stage: touchdown detection, landing and air collision; consumes the stability stat. Probes via `0x0013A7B0`; on touchdown pushes the rider with `0x00106538`, resolves the contact and classifies the landing: crash via `0x0010EB30`, or ordinary landing via `0x0010E910`, which passes |rider+0x1E0| (taken before the response) and the probe point/normal/surface to `0x00111AA0` and plays landing sound `0x002948D0`. Then sets body mask +0x28 to 3 (word `0x004A1120`, spheres 0/1) for `0x0013AA48` and 0xFFFFFFFF for `0x00105398` (a1 = bounced hit normal or 0). A landing out of control 3 hands off to `0x0011FEC8`, keeping spin +0x2DC and the prewind triplets. *[PS2]*
  - `0x00139D54`: f22 holds |rider+0x1E0| captured before the landing response (the snow impact strength). *[static]*
  - `0x00139D78`: Landing contact resolver block (to `0x0013A148`): sets touchdown position/velocity and normal plus previous normal, keeps tangential position and material depth, applies the material impulse and normal-speed clamp (surface 0 cancels ~-842 cm/s). *[static]*
  - `0x0013A14C`: Landing classification block (to `0x0013A4CC`) from animation class/flags, orientation, manual state and the landing stat: inverted/hard landings go to crash (control 8 / motion 2); the crash semantic 351/352/353 comes from (RNG word & 3). *[PS2]*
  - `0x0013A2E0`: Landing response block: requests a landing clip on hard impacts below -1388.888916 cm/s. *[static]*
  - `0x0013A594`: Landing path (to `0x0013A5D0`) that keeps control 2; only an impact speed < -1388.888916 cm/s requests the landing animation. *[static]*
  - `0x0013A5D4`: Landing branch: with +0x330 != 0 plays 26/34 and re-enters control 1, skipping `0x00115640`, the reverse turn and the landing animation. *[PS2]*
  - `0x0013A734`: Stores the core sphere mask (word `0x004A1120` = 3) into body+0x28 before `0x0013AA48`. *[PS2]*
- **`0x0013A7B0`** - Board landing contact query (the complete touchdown terrain query): segment centre is the posed board-root bone chosen by rider+0x8A0 (22 for Zoe), endpoints centre -/+ 200 cm along presentation up rider+0x180; query mode 0, kind 2 (coarse + 4 Newton), refined terrain with the contact cache. Picks the coarse fraction nearest 0.575 with the generic instance/terrain tie break (no cruise normal preference) and returns the contact including patch flags. The landing impact gate needs coarse fraction >= 0.5 and negative contact-normal velocity. *[PS2]*
- **`0x0013A8F8`** - Landing clip play routine: calls `0x003128E8` with a2 = 0, which revives a still-fading copy. *[PS2]*
- **`0x0013A968`** - Ordinary snow landing classifier/player (semantics 61/62/63/66/67), not rail; reads the retained manual spin +0x2DC and plays with a2 = 0 so fading copies revive. *[PS2]*
- **`0x0013AA48`** - Air body collision phase (air counterpart of `0x0013F488`), run by `0x00139C88` with core mask 3 so only spheres 0/1 are queried, after the touchdown push `0x00106538`. Pushes the body from the cached pre-landing spheres (no re-pose; rider+0x868 body cache, authored surface filter 2), translating actor position and cached sphere centres, then applies the air bounce; `0x00105398` follows with a1 = the bounced hit normal (else 0). Ignores surface -1 scenery. After a landing crash it moves the crash actor and `0x00105D98` dispatches a ragdoll impact. *[PS2]*
- **`0x0013AD20`** - Rail motion (motion 4) enter, run immediately inside requestMotion(4): bakes presentation into the root with `0x0011FA10`(rider, rider+0x110) (no `0x0011EB98` lift), rebuilds via `0x0011E098`, zeroes the +0x1F0/+0x208/+0x250 triplets, +0x2DC and rail lean/time/balance/lost/heading, sets +0x25C rate 0.05 target 0, copies the entry position and zeroes word +0x40. *[PS2]*
- **`0x0013ADC0`** - Rail heading offset, the last rail-attach step (after `0x00115358`): angle of the rebuilt forward vs the rail direction in the rail plane, using the normal from `0x0013BD80`, lateral = normal x direction, atan2(dot(fwd,lat), dot(fwd,dir)) clamped to +-0.7853982 (45 deg). *[PS2]*
- **`0x0013AF28`** - Rail motion (motion 4) update, dt = timeScale/60: re-queries the rail at the board bone (r = 300 cm; failure rebuilds) and re-sets the rider+0x5AC nearby-entity flag only when the hit instance has an entity whose vtable slot +0x170 predicate is nonzero (static spline rails have none). f20 = 0.5, or |v|*0.0009 when |v| >= 555.5555; a `0x001086B8` failure gives a 277.7778 detach push. Vertical correction clamp +-1000*dt; accel = tangent*dot((0,0,-980), t) +- 2450*boost; slide = steer*180*clamp(f20, .5, 2); imbalance 0.3 (styles 1/2) or 1.8 (3/4), /(1 + 1.650076*stability stat). Lean/heading by style, then `0x00121AA0` and `0x0011E098`. *[PS2]*
  - `0x0013B054`: Stores the rail record header packed id (header+0x00) into rail motion+0x24 on each hit. *[static]*
  - `0x0013B1D8`: Loads 1000.0 (0x447A0000) for the vertical-correction clamp [-1000*dt, 1000*dt]; +-300*dt was a wrong earlier reading. *[static]*
  - `0x0013B66C`: Tests rail descriptor flag bit 2 (header+0x1C): when set the imbalance term is doubled; only otherwise does |lateral| < 2.5 cap it at -1. *[static]*
- **`0x0013BD80`** - Rail contact normal: for |tangent.z| <= 0.92, normalize(Z - t*t.z) sign-matched to the rider up; steeper rails use lateral = normalize(up x t), perp = t x lateral, normal = perp*cos(lean) - lateral*sin(lean). *[static]*
- **`0x0013BFA8`** - Rail motion (motion 4) post stage, run every rail tick after the pose (not only on loss); plays no animation. Order: `0x0011E150`; if lost, steer target 0 at rate 1/30 and `0x00114298`(-1); then trigger/physical/pair contact phases on the posed body with temporary body masks: table `0x00458230` mask before `0x00105398`, mask 3 before `0x0013C140`, 0xFFFFFFFF before `0x00107888` (the incoming mask is not restored). If lost and still in motion 4 it requests motion 1 (no rotation input -> control 4, held rotation -> control 5) and plays take-off sound `0x00294170`; ends with a speed clamp. *[PS2]*
  - `0x0013C078`: Sets body mask 3 before the physical contact response `0x0013C140`. *[static]*
  - `0x0013C094`: Sets body mask 0xFFFFFFFF before the pair phase `0x00107888`. *[static]*
  - `0x0013C098`: Rider pair point slot in the rail post. *[static]*
- **`0x0013C140`** - Rail-exit physical contact response, referenced only by `0x0013BFA8` (after `0x00105398`): body query with mask 3 and no `0x0013F488`-style normal filter (built at `0x0013C1A0`), rejecting only opposed contacts below -0.9999. Projects/normalises negative-alignment normals, translates by 1.1*depth (+0xAA0 body translation, +0x9D0 offset accumulation), applies `0x00106538` and `0x0011E098`, and tracks rail motion +0x30/+0x40 (previous position / stationary time): 0.5 s within 2 cm pushes at 1111.111 cm/s. Rebound max(0.5*closing, 27.77778) with direction/normal/impulse notification. *[PS2]*
- **`0x0013C5A0`** - Rail motion (motion 4) exit: runs the rail-exit tolerance ramp (rail exits use this, not `0x0013F410`). Also run on a select warp from a rail, together with the control-7 exit `0x00132048`. *[PS2]*
- **`0x0013C7A8`** - Ground motion (motion 0) entry, used on a clean landing and by crash get-up: resets the board bounce oscillator, scales depths, sets boardNormal = normal, stamps the absolute ground-focus tick owner+0x10 (an off-by-one changes re-jump launch speed) and multiplies each velocity lane by 0.7f (`0x3F333333`) if tick - lastLeave <= 40, else min(0.7 + (d-40)*`0x3C23D70B` (~0.01), 1). After a crash it is the path that finally rewrites +0x380/+0x3A0/+0x3B0, which stay at their entry values during motion 2. *[PS2]*
- **`0x0013C878`** - Ground normal-response acceleration and compression ratio (d = contact distance, vn = outward normal speed, h1/h3 smoothed depths, G gravity, D damping): d > 0: a = G*(d*-0.033333), minus D*vn only if vn > 0; -h1 < d <= 0: a = (-G*d)/h1 - D*vn; deeper: d = max(-h3, d), a = G*(1 - 2*(d+h1)/(h3-h1)) - D*vn. Writes max(-d/h3, 0) to rider+0x758. *[static]*
- **`0x0013C948`** - Ground forward drive (consumes the acceleration stat): auto boost / fall-line speed gain from the route heading +0x4CC, factor 1 - |atan(forward) - heading|/limit when the board points within the limit of the path direction, plus crouch and animation-state effects. Patch flag 0x10 (rider+0x2D4) forces the heading boost; half pipes scale the auto boost by |normal.z| with a push-off gap for patch locations 11..13; location >= 17 halves it. *[PS2]*
  - `0x0013C9CC`: Reads rider+0x2D4 patch flag 0x10 to force the heading boost. *[PS2]*
- **`0x0013CCF0`** - Ground longitudinal friction/braking with surface depth factors, stance and stat factors (top speed, acceleration and edging stats via the player/stat helpers). *[static]*
- **`0x0013D028`** - Ground lateral response with a speed curve; consumes the edging stat. *[static]*
- **`0x0013D1B8`** - Cruise ground terrain contact query: copies rider+0x370 to +0x380 (previous normal); centre = position + previous lateral tangent (+0x3B0) x 45 cm x filteredTurn x geometryScale (Zoe 0.85), endpoints centre -100/+200 cm along the previous normal (not renormalized), cache rider+0x864. Writes +0x430 packed patch, +0xAAC/+0xAB0 UV, +0x460 point, +0x454 signed distance, +0x438 surface (-1/missing -> 0), and copies the patch runtime flags into rider+0x2D4. *[PS2]*
  - `0x0013D20C`: Copies the current contact normal into the previous normal (with `0x0013D218`); not reached before controller takeoff, so air frames keep the historical normal. *[static]*
  - `0x0013D218`: Continuation of the previous-normal update. *[static]*
  - `0x0013D42C`: Cruise candidate selection: prefers normals with dot(previous normal) >= 0.3, then minimal |coarseFraction - 0.5|, ties by packed resource id; differs from the generic `0x003342D0` selection. *[static]*
  - `0x0013D604`: Store of the patch runtime flags to rider+0x2D4. *[PS2]*
  - `0x0013D64C`: Contact step that changes the rider surface id. *[static]*
  - `0x0013D7F8`: Contact clearance block: length of the tangential part of (query centre - surface point). *[static]*
- **`0x0013D818`** - Normal cruise ground motion update (motion mode 0). Calls `0x00125970` first, clamps speed to rider+0x2E4, computes dt = rider+0x300 * (1/60), caches the surface property pointer, projects velocity minus surface velocity onto normal/forward/lateral axes and assembles forces from `0x0013C878` (normal), `0x0013C948` (forward drive), `0x0013CCF0` (forward friction), `0x0013D028` (lateral) plus gravity (0,0,-G); integrates position with the old velocity, then velocity. Then sets heading, lift, turn/brake/crouch and board-alignment targets (reading the animation class), calls contact update `0x0013D1B8` and ends with orientation rebuild `0x0011E098`. Also consumes the edging stat. *[PS2]*
  - `0x0013D89C`: Reads the cruise speed limit rider+0x2E4 for the initial speed clamp (through `0x0013D8C4`). *[static]*
  - `0x0013D8E0`: Computes dt = rider+0x300 * (1/60) (through `0x0013D8F0`). *[static]*
  - `0x0013D8F4`: Caches the current surface property pointer before movement. *[static]*
  - `0x0013D9E4`: Reads the retained surface basis rider+0x3A0/+0x3B0 (also at `0x0013DA14`, `0x0013DD40`); the basis is kept from ground contact, not rebuilt from the aligned quaternion. *[static]*
  - `0x0013DA38`: Calls normal response `0x0013C878` (controller, surface record, contact distance, normal relative speed). *[static]*
  - `0x0013DA48`: Calls forward drive term `0x0013C948` (controller, surface record). *[static]*
  - `0x0013DA64`: Calls forward friction `0x0013CCF0` (controller, surface, forward relative speed, normalised normal term). *[static]*
  - `0x0013DA80`: Calls lateral term `0x0013D028` (controller, surface, lateral speed, forward speed, -rider+0x1F0). *[static]*
  - `0x0013DCD0`: Adds vertical gravity (0,0,-surfaceGravity) to the assembled acceleration (through `0x0013DCE4`). *[static]*
  - `0x0013E0A8`: Integrates position first, using the old velocity (through `0x0013E0DC`). *[static]*
  - `0x0013E0E8`: velocity += acceleration*dt (through `0x0013E120`); end of the cruise translation prefix. *[static]*
  - `0x0013E144`: Presentation lift (+0x2C8) target (through `0x0013E228`): old/new position along the filtered board normal, clamped by crouch, times body scale; suppressed by animation classes 10/4/5 or semantic 22. *[static]*
  - `0x0013E22C`: Cruise heading stage (through `0x0013EB98`): velocity alignment, turn/charge bias and +0x2DC transient angle from the pre-integration velocity; state 2 adds a normal.z > 0.65 gate. Uses curves `0x004A1130`, `0x004A1138`, `0x004A1158`. *[static]*
  - `0x0013EBA0`: Calls contact update `0x0013D1B8`. *[static]*
  - `0x0013EBF4`: Post-contact velocity correction (through `0x0013ED24`): for new surface ids other than 2/3/13 adds normal*(-0.4*dot(relVel,normal)), clamps the added Z to >= -40 cm/s, keeps relative speed, then adds surface velocity. *[static]*
  - `0x0013ED24`: Ground contact correction block in the cruise path (not a separate function). *[static]*
  - `0x0013ED28`: Passive takeoff height block; reads the old cached surface (+0x10/+0x38). *[static]*
  - `0x0013ED68`: Ground surface-alignment stage (through `0x0013EE9C`), only when contact distance < 5 cm: target = normalize(n.x, n.y, 1.5*n.z); if |cross(target, board up)| > 0.001 rotates by -asin(min(sine,1)) * (1/60) * surface+0x38. *[static]*
  - `0x0013EE84`: Alignment block reading the old cached surface (+0x10/+0x38). *[static]*
  - `0x0013EEA0`: Ground tail (through `0x0013F064`): restores board-alignment targets after the landing animation class (rate dt*3; classes 10/5, semantics 2/22 disable it); extra lean uses curve `0x004A1128` (gp-0x1FC8) only on surfaces 2/3 outside control 1. *[static]*
  - `0x0013F11C`: Final unconditional orientation rebuild call `0x0011E098`. *[static]*
- **`0x0013F178`** - Ground motion post/collision driver. Handles the patch-flag 0x2 reset (also via surface +0x44) with `0x00116120(rider,0,1)`; when the departure flag is set, calls takeoff `0x00114298(rider,-1)` first; writes board sound inputs (speed, brake, turn, compression). Sets body+0x28 sphere mask to 3 (word `0x004A115C`) for obstacle phase `0x0013F488`, restores 0xFFFFFFFF for soft collision `0x00105398`, runs pair collision `0x00107888`, then if departing and `0x0011FE98()==0` calls `0x0011FE78(rider,1)` to switch to air motion. Also applies a near-vertical +4 cm push after the pose, updates the filtered board normal +0x390 and applies the final speed clamp. *[PS2]*
  - `0x0013F194`: Passive takeoff call `0x00114298(rider,-1)` (through `0x0013F1A0`); the last ground body query point before the switch to the air predictor. *[static]*
  - `0x0013F210`: Sets ground-leave targets (with `0x001399E0`) before `0x001211F8` advances presentation filters on a charged release; first airborne pose has board alignment 0.95 and body lift 2.0384 cm. *[static]*
  - `0x0013F248`: Reads patch flag 0x2 and requests `0x00116120(rider,0,1)` while grounded. *[PS2]*
  - `0x0013F264`: Passive-departure ordering point between the final ground query and air predictor initialization. *[static]*
  - `0x0013F278`: Stores core sphere mask word `0x004A115C` (3) into body+0x28 before `0x0013F488`; the mask returns to 0xFFFFFFFF before `0x00105398`. *[PS2]*
  - `0x0013F2B0`: Passive departure check (through `0x0013F2CC`), after `0x00105398` and `0x00107888`: if the departure flag is set and `0x0011FE98()==0`, calls `0x0011FE78(rider,1)`, initializing the airborne predictor after final clamping. *[PS2]*
  - `0x0013F2CC`: Departure switch to air motion, reaching `0x001399E0`; a `0x00105398` bounce on a departure tick seeds the flight here. A crash entered on a departure tick starts sliding from previous motion 0. *[PS2]*
  - `0x0013F2E4`: Writes next-tick filtered board normal rider+0x390 = normalize(old +0x390 + 0.5*current +0x370) (through `0x0013F354`), after pose queries that still use the old value. *[PS2]*
  - `0x0013F358`: Final ground speed clamp block (through `0x0013F3A8`), distinct from the initial clamp; runs after `0x0013F488`, `0x00105398` and rider-pair dispatch, so contacts resolve on the unclamped velocity. *[PS2]*
- **`0x0013F410`** - Ground (motion 0) exit: re-arms decays of the board-alignment and extra-lean targets +0x208/+0x2BC/+0x2C8 toward 0 (rate 0.05; constants 0x3D4CCCCE..0x3FD55556), stamps motion owner+0x14 and records the last-leave tick (tick-500 if unset, else the current tick from `0x001298C8`). Run by motion requests leaving the ground (rail ground attach, crash entry); rail exits use `0x0013C5A0` instead. *[PS2]*
- **`0x0013F488`** - Ground (cruise) obstacle collision phase, called from `0x0013F178`: queries body spheres 0/1 (mask 3) against terrain and surfaced instances using a query built by `0x0032F650` with the normal-direction filter, ground normal and node filter 2 (solid surface -1 scenery is left to `0x00105398`), then applies linear push-out/bounce. Skipped in control state 9 and when rider+0x874==0, rider+0x4C8<50 and |pos.z - rider+0x498| < 50 cm. Separating contacts only translate (no bounce, `0x001065B0` or `0x00105D98`). Calls `0x002989A8`. *[PS2]*
  - `0x0013F4CC`: Skip test: a computer rider within 50 cm of its route (rider+0x874 == 0) skips this query; `0x00105398` and `0x00107888` still run. *[PS2]*
  - `0x0013F5C4`: Linear response block (through `0x0013F7C4`): rejects normals nearly opposite the ground normal; push = normal*1.1*penetration; closing velocity adds closing + max(0.05*closing, 27.78 cm/s); then `0x001065B0`, quaternion rebuild, `0x00105D98`. *[static]*
- **`0x001408F0`** - Rider virtual target-position getter (vtable slot +0x28): returns rider+0x110. *[static]*
- **`0x00140B80`** - Returns the rider's environment index rider+0x86C (rider interface at +0x6C0, slot +0x38, this-adjust -0x6C0). The index selects the rider's lighting environment block and indexes table `0x004FA398` (stride 0xF0) of ARGB float4 colours used for board-track vertices. *[static]*
- **`0x00140BC0`** - Path-end predicate for AI/reset path records. *[static]*
- **`0x00140BC8`** - Side-effect-free virtual getter; called in `0x001200D0` with the result unused. *[static]*
- **`0x00140C28`** - Getter for rider+0x3FC. *[static]*
- **`0x00140C30`** - Setter for rider+0x3FC (dispatch path unconfirmed). *[static]*
- **`0x00144670`** - Moves sequence marker flag bit 63 from the latched word seq+0xB0 into the newly raised word seq+0xB8. *[static]*
- **`0x001446A0`** - Tests a bit in an animation sequence's latched flag word seq+0xB0 (seq, bit). Used by rail attach class gates and uber phase 2 (event bit 0). Paired with `0x001446B8`; which of the two returns latched vs newly raised bits is inferred from listing order. *[static]*
- **`0x001446B8`** - Sequence marker query, probably for newly raised marker bits; paired with the latched-bit query `0x001446A0` (split inferred from listing order). *[unconfirmed]*
- **`0x001446E8`** - Human rider route score (vtable +0xA4). *[static]*
